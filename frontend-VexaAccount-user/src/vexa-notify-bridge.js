(()=>{'use strict';
const ensure=()=>{let s=document.querySelector('.toast-stack');if(!s){s=document.createElement('div');s.className='toast-stack';s.setAttribute('aria-live','polite');s.setAttribute('aria-atomic','true');document.body.appendChild(s)}return s};
window.vexaNotify=(message,type='info',duration)=>{const stack=ensure(),n=document.createElement('div');n.className='vx-toast '+type;n.setAttribute('role',type==='error'?'alert':'status');n.innerHTML=`<div class="vx-toast-inner"><span class="vx-toast-icon">${type==='success'?'✓':type==='error'?'×':type==='warning'?'!':'i'}</span><div class="vx-toast-message"></div><button class="vx-toast-close" type="button" aria-label="Close notification">×</button></div><div class="vx-toast-progress"></div>`;n.querySelector('.vx-toast-message').textContent=String(message);n.querySelector('.vx-toast-close').onclick=()=>n.remove();stack.appendChild(n);const ms=duration??(type==='error'?5000:type==='info'?3000:4000);n.querySelector('.vx-toast-progress').style.setProperty('--vx-toast-duration',`${ms}ms`);const timer=setTimeout(()=>n.remove(),ms);n.querySelector('.vx-toast-close').addEventListener('click',()=>clearTimeout(timer),{once:true});return n};
})();
