const API_BASE=window.VEXA_ACCOUNT_API_BASE||'https://api-vexaaccount.onrender.com';
const api=(p,o={})=>fetch(API_BASE+p,{credentials:'include',...o,headers:{'Content-Type':'application/json',...(o.headers||{})}});async function json(p,o){const r=await api(p,o),d=await r.json().catch(()=>({}));if(!r.ok||d.success===false)throw Error(d.message||d.error||`Request failed: ${r.status}`);return d}const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const root=()=>document.getElementById('app');let state={session:null,profile:null,security:{},prefs:{},credits:{},apps:[],sessions:[],events:[],storage:[],view:'overview'};
function authLayout(t,s,b){root().innerHTML=`<main class="auth-shell"><section class="auth-card glass"><a class="auth-brand" href="/#/login"><img src="./public/brand.svg" alt="VexaAccount"></a><p class="eyebrow">VEXA ACCOUNT</p><h1>${t}</h1><p class="muted">${s}</p>${b}</section></main>`}function msg(t,c=''){const e=document.getElementById('authMessage');if(e){e.textContent=t;e.className=`auth-message ${c}`}}
function login(){authLayout('Welcome back','Sign in once to access all Vexa apps.',`<form id="authForm" class="auth-form"><label>Email<input name="email" type="email" autocomplete="email" required placeholder="you@example.com"></label><label>Password<input name="password" type="password" autocomplete="current-password" minlength="8" required placeholder="Enter your password"></label><div class="auth-row"><a href="#/forgot-password">Forgot password?</a></div><button class="primary auth-submit">Sign in</button><p id="authMessage" class="auth-message" role="alert"></p></form><div class="auth-links">Don't have an account? <a href="#/register">Create one</a></div><div class="auth-security"><b>Security</b><span>Your VexaAccount works across all connected Vexa apps.</span></div>`);document.getElementById('authForm').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget),b=e.currentTarget.querySelector('button');b.disabled=true;msg('Signing in securely…');try{const d=await json('/api/auth/login',{method:'POST',body:JSON.stringify({email:String(f.get('email')).trim(),password:String(f.get('password'))})});if(d.requiresAuthenticator2fa)return twofa('authenticator',d.userId);if(d.requiresEmail2fa)return twofa('email',d.userId,String(f.get('email')).trim());location.hash='#/';await load()}catch(x){msg(x.message,'error');b.disabled=false}}}
function register(){authLayout('Create your VexaAccount','One secure identity for every connected Vexa app.',`<form id="authForm" class="auth-form"><label>Full name<input name="name" autocomplete="name" required></label><label>Email<input name="email" type="email" autocomplete="email" required></label><label>Password<input name="password" type="password" autocomplete="new-password" minlength="8" required></label><label>Confirm password<input name="confirm" type="password" autocomplete="new-password" minlength="8" required></label><button class="primary auth-submit">Create account</button><p id="authMessage" class="auth-message" role="alert"></p></form><div class="auth-links">Already have an account? <a href="#/login">Sign in</a></div>`);document.getElementById('authForm').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget),p=String(f.get('password')),b=e.currentTarget.querySelector('button');if(p!==String(f.get('confirm')))return msg('Passwords do not match.','error');b.disabled=true;msg('Creating your account…');try{await json('/api/auth/register',{method:'POST',body:JSON.stringify({name:String(f.get('name')).trim(),email:String(f.get('email')).trim(),password:p})});location.hash=`#/verify-email?email=${encodeURIComponent(String(f.get('email')).trim())}`}catch(x){msg(x.message,'error');b.disabled=false}}}
function verify(email){authLayout('Verify your email','Enter the 6-digit code sent to your email.',`<form id="authForm" class="auth-form"><label>Email<input name="email" value="${esc(email)}" readonly></label><label>Verification code<input name="otp" inputmode="numeric" autocomplete="one-time-code" maxlength="6" required placeholder="000000"></label><button class="primary auth-submit">Verify email</button><p id="authMessage" class="auth-message" role="alert"></p></form><div class="auth-links"><a href="#/login">Back to sign in</a></div>`);document.getElementById('authForm').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget),b=e.currentTarget.querySelector('button');b.disabled=true;msg('Verifying…');try{await json('/api/auth/verify-otp',{method:'POST',body:JSON.stringify({email:String(f.get('email')),otp:String(f.get('otp')).trim()})});location.hash='#/';await load()}catch(x){msg(x.message,'error');b.disabled=false}}}
function twofa(type,id,email=''){authLayout('Verify your sign-in',type==='email'?'Enter the code sent to your email.':'Enter the 6-digit code from your authenticator app.',`<form id="authForm" class="auth-form"><label>${type==='email'?'Email verification code':'Authenticator code'}<input name="otp" inputmode="numeric" autocomplete="one-time-code" maxlength="6" minlength="6" required placeholder="000000"></label><button class="primary auth-submit">Verify and continue</button><p id="authMessage" class="auth-message" role="alert"></p></form><div class="auth-links"><a href="#/login">Cancel and return to sign in</a></div>`);document.getElementById('authForm').onsubmit=async e=>{e.preventDefault();const otp=String(new FormData(e.currentTarget).get('otp')).trim(),b=e.currentTarget.querySelector('button');b.disabled=true;msg('Checking verification code…');try{await json(type==='email'?'/api/auth/verify-email-2fa':'/api/auth/twofa/verify',{method:'POST',body:JSON.stringify(type==='email'?{userId:id,email,otp}:{userId:id,token:otp})});location.hash='#/';await load()}catch(x){msg(x.message,'error');b.disabled=false}}}
function forgot(){authLayout('Reset your password','Enter your email and we will send a secure reset link.',`<form id="authForm" class="auth-form"><label>Email<input name="email" type="email" autocomplete="email" required placeholder="you@example.com"></label><button class="primary auth-submit">Send reset link</button><p id="authMessage" class="auth-message" role="alert"></p></form><div class="auth-links"><a href="#/login">Back to sign in</a></div>`);document.getElementById('authForm').onsubmit=async e=>{e.preventDefault();const email=String(new FormData(e.currentTarget).get('email')).trim(),b=e.currentTarget.querySelector('button');b.disabled=true;msg('Sending reset link…');try{const d=await json('/api/auth/forgot-password',{method:'POST',body:JSON.stringify({email})});msg(d.message||'If your email is registered, you will receive a reset link.','success')}catch(x){msg(x.message,'error')}finally{b.disabled=false}}}
function reset(token){if(!token)return forgot();authLayout('Choose a new password','Create a strong new password for your VexaAccount.',`<form id="authForm" class="auth-form"><label>New password<input name="password" type="password" autocomplete="new-password" minlength="8" required></label><label>Confirm password<input name="confirm" type="password" autocomplete="new-password" minlength="8" required></label><button class="primary auth-submit">Reset password</button><p id="authMessage" class="auth-message" role="alert"></p></form>`);document.getElementById('authForm').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget),p=String(f.get('password')),b=e.currentTarget.querySelector('button');if(p!==String(f.get('confirm')))return msg('Passwords do not match.','error');b.disabled=true;msg('Resetting password…');try{const d=await json('/api/auth/reset-password',{method:'POST',body:JSON.stringify({token,newPassword:p})});msg(d.message||'Password reset successfully.','success');setTimeout(()=>location.hash='#/login',1200)}catch(x){msg(x.message,'error');b.disabled=false}}}
function route(){const raw=location.hash||'';const [r,q='']=raw.split('?'),p=new URLSearchParams(q);if(r==='#/login'||r==='#login')return login();if(r==='#/register'||r==='#register')return register();if(r==='#/verify-email')return verify(p.get('email')||'');if(r==='#/forgot-password')return forgot();if(r==='#/reset-password')return reset(p.get('token'));return false}
async function load(){if(route())return;const s=await json('/api/auth/session');if(!s.success)return login();state.session=s;const rs=await Promise.allSettled(['/api/account/profile','/api/account/security','/api/account/preferences','/api/account/credits','/api/account/storage','/api/account/apps','/api/account/sessions','/api/account/security/events'].map(x=>json(x)));const v=rs.map(x=>x.status==='fulfilled'?x.value:{});const [p,sec,prefs,cr,st,apps,ses,ev]=v;state.profile=p.user||s.user||{};state.security=sec.security||{};state.prefs=prefs.preferences||{};state.credits=cr.balance||{};state.storage=st.records||[];state.apps=apps.applications||[];state.sessions=ses.sessions||[];state.events=ev.events||[];render()}
function nav(){return [['overview','Overview'],['profile','Profile'],['security','Security'],['apps','Applications'],['sessions','Sessions'],['storage','Cloud storage'],['credits','Credits & coins'],['preferences','Preferences']].map(x=>`<button class="nav ${state.view===x[0]?'active':''}" data-view="${x[0]}">${x[1]}</button>`).join('')}
function shell(c){const u=state.profile||state.session.user||{};root().innerHTML=`<div class="console"><aside class="sidebar"><div class="brand"><span class="brand-mark">V</span><div><b>VexaAccount</b><small>ACCOUNT CENTER</small></div></div><nav>${nav()}</nav><div class="side-footer"><span class="owner-dot"></span><div><b>${esc(u.name||'Account')}</b><small>${esc(u.email||'')}</small></div></div></aside><section class="workspace"><header class="topbar"><div><p class="eyebrow">VEXA ACCOUNT</p><h1>Account Center</h1></div><div class="top-actions"><span class="live"><i></i> System online</span><button id="switchAccount" class="secondary">Switch account</button><button id="logout" class="secondary">Sign out</button></div></header><main class="content">${c}</main><nav class="bottom-nav">${nav()}</nav></section></div>`;document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>{state.view=b.dataset.view;render()});document.getElementById('logout').onclick=logout;document.getElementById('switchAccount').onclick=logout}async function logout(){try{await api('/api/auth/logout',{method:'POST'})}finally{location.hash='#/login'}}
function card(t,b){return `<section class="panel glass"><h2>${t}</h2>${b}</section>`}function overview(){const u=state.profile||{};return `<section class="hero glass"><div><p class="eyebrow">CENTRAL IDENTITY PLATFORM</p><h2>Your VexaAccount, connected everywhere.</h2><p>Manage your identity, security, applications, sessions, storage and account preferences from one place.</p></div></section><section class="metrics"><article class="metric glass"><small>Connected applications</small><strong>${state.apps.length}</strong></article><article class="metric glass"><small>Active sessions</small><strong>${state.sessions.length}</strong></article><article class="metric glass"><small>Credit score</small><strong>${esc(state.credits.credit_score??0)}</strong></article><article class="metric glass"><small>Coins</small><strong>${esc(state.credits.coins??0)}</strong></article></section><section class="grid">${card('Account status',`<p><b>${esc(u.email||'')}</b></p><p class="muted">Status: ${u.is_active?'Active':'Disabled'} · Verified: ${u.is_verified?'Yes':'No'}</p>`)}${card('Recent security activity',state.events.slice(0,6).map(e=>`<article class="row"><div><b>${esc(e.event_type||'Security event')}</b><small>${esc(e.created_at||'')} · ${esc(e.ip_address||'')}</small></div></article>`).join('')||'<p class="muted">No recent security activity.</p>')}</section>`}function profile(){const u=state.profile||{};return card('Profile',`<form id="profileForm" class="form-grid"><label>Name<input name="name" value="${esc(u.name)}"></label><label>First name<input name="first_name" value="${esc(u.first_name)}"></label><label>Last name<input name="last_name" value="${esc(u.last_name)}"></label><label>Country<input name="country" value="${esc(u.country)}"></label><label>Gender<input name="gender" value="${esc(u.gender)}"></label><label>Date of birth<input name="dob" type="date" value="${esc(u.dob?String(u.dob).slice(0,10):'')}"></label><button class="primary" type="submit">Save profile</button></form><hr><h3>Change email</h3><form id="emailForm" class="form-grid"><input name="email" type="email" value="${esc(u.email)}" required><input name="password" type="password" placeholder="Current password" required><button class="primary" type="submit">Change email</button></form><hr><h3>Change password</h3><form id="passwordForm" class="form-grid"><input name="currentPassword" type="password" placeholder="Current password" required><input name="newPassword" type="password" minlength="8" placeholder="New password" required><button class="primary" type="submit">Change password</button></form>`)}function security(){return card('Security',`<p>Authenticator 2FA: <strong>${state.security.twofa_enabled?'Enabled':'Disabled'}</strong></p><p>Email 2FA: <strong>${state.security.email_2fa_enabled?'Enabled':'Disabled'}</strong></p><button id="disable2fa" class="secondary">Disable 2FA</button><button id="disablePasscode" class="secondary">Disable passcode</button><hr>${state.events.map(e=>`<article class="row"><div><b>${esc(e.event_type||'Event')}</b><small>${esc(e.created_at||'')}</small></div></article>`).join('')||'<p class="muted">No events.</p>'}`)}function apps(){return card('Connected applications',state.apps.map(a=>`<article class="app-card"><div class="app-main"><div class="app-icon">${esc((a.display_name||a.name||a.client_id||'A').slice(0,1).toUpperCase())}</div><div><h3>${esc(a.display_name||a.name||a.client_id)}</h3><small>${esc(a.client_id)} · ${esc(a.status||'active')}</small></div></div><button data-revoke="${esc(a.client_id)}" class="danger">Revoke access</button></article>`).join('')||'<p class="muted">No connected applications.</p>')}function sessions(){return card('Active sessions',state.sessions.map(s=>`<article class="row"><div><b>${esc(s.device||s.user_agent||'Browser session')}</b><small>${esc(s.ip_address||'')} · ${esc(s.last_seen_at||s.created_at||'')}</small></div><button data-session="${esc(s.id)}" class="secondary">Revoke</button></article>`).join('')||'<p class="muted">No active sessions.</p>')}function storage(){return card('Cloud storage',state.storage.map(r=>`<article class="row"><div><b>${esc(r.display_name||r.storage_key)}</b><small>${esc(r.provider||'')} · ${esc(r.size_bytes||0)} bytes</small></div><button data-storage="${esc(r.id)}" class="danger">Remove</button></article>`).join('')||'<p class="muted">No storage records.</p>')}function credits(){return `<section class="metrics"><article class="metric glass"><small>Credit score</small><strong>${esc(state.credits.credit_score??0)}</strong></article><article class="metric glass"><small>Coins</small><strong>${esc(state.credits.coins??0)}</strong></article></section>`}function preferences(){const p=state.prefs||{};return card('Preferences',`<form id="prefsForm" class="form-grid"><label>Language<input name="locale" value="${esc(p.locale||'en')}"></label><label>Timezone<input name="timezone" value="${esc(p.timezone||'UTC')}"></label><label><input name="marketing_email_enabled" type="checkbox" ${p.marketing_email_enabled?'checked':''}> Marketing emails</label><label><input name="security_email_enabled" type="checkbox" ${p.security_email_enabled!==0?'checked':''}> Security emails</label><button class="primary">Save preferences</button></form><hr><button id="deleteAccount" class="danger">Delete my account</button>`)}
async function save(e,p,m){e.preventDefault();const b=e.currentTarget.querySelector('button');b.disabled=true;try{await json(p,{method:m,body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});await load()}catch(x){alert(x.message);b.disabled=false}}function bind(){const p=document.getElementById('profileForm');if(p)p.onsubmit=e=>save(e,'/api/account/profile','PATCH');const em=document.getElementById('emailForm');if(em)em.onsubmit=e=>save(e,'/api/account/email','PATCH');const pw=document.getElementById('passwordForm');if(pw)pw.onsubmit=e=>save(e,'/api/account/password','PATCH');const d2=document.getElementById('disable2fa');if(d2)d2.onclick=async()=>{const password=prompt('Enter your password');if(password)try{await json('/api/account/security/disable-2fa',{method:'POST',body:JSON.stringify({password})});await load()}catch(x){alert(x.message)}};const dp=document.getElementById('disablePasscode');if(dp)dp.onclick=async()=>{const password=prompt('Enter your password');if(password)try{await json('/api/account/security/disable-passcode',{method:'POST',body:JSON.stringify({password})});await load()}catch(x){alert(x.message)}};const pf=document.getElementById('prefsForm');if(pf)pf.onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget);try{await json('/api/account/preferences',{method:'PATCH',body:JSON.stringify({locale:f.get('locale'),timezone:f.get('timezone'),marketing_email_enabled:f.has('marketing_email_enabled'),security_email_enabled:f.has('security_email_enabled')})});await load()}catch(x){alert(x.message)}};const da=document.getElementById('deleteAccount');if(da)da.onclick=async()=>{const password=prompt('Enter your password');if(password&&confirm('Permanently delete this account?'))try{await json('/api/account/account',{method:'DELETE',body:JSON.stringify({password})});await logout()}catch(x){alert(x.message)}}}
function render(){const views={overview,profile,security,apps,sessions,storage,credits,preferences};shell(views[state.view]());bind();document.querySelectorAll('[data-revoke]').forEach(b=>b.onclick=async()=>{if(confirm('Revoke this application?'))try{await json(`/api/account/apps/${encodeURIComponent(b.dataset.revoke)}/consent`,{method:'DELETE'});await load()}catch(x){alert(x.message)}});document.querySelectorAll('[data-session]').forEach(b=>b.onclick=async()=>{try{await json(`/api/account/sessions/${encodeURIComponent(b.dataset.session)}`,{method:'DELETE'});await load()}catch(x){alert(x.message)}});document.querySelectorAll('[data-storage]').forEach(b=>b.onclick=async()=>{if(confirm('Remove this storage record?'))try{await json(`/api/account/storage/${encodeURIComponent(b.dataset.storage)}`,{method:'DELETE'});await load()}catch(x){alert(x.message)}})}
window.addEventListener('hashchange',load);load().catch(e=>{root().innerHTML=`<main class="shell loading-shell"><section class="card glass loading-card"><div class="brand-mark">V</div><p class="eyebrow">VEXA ACCOUNT</p><h1>Unable to load Account Center</h1><p class="muted">${esc(e.message)}</p><button class="primary" onclick="location.reload()">Retry</button></section></main>`});
