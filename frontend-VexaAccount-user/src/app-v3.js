(()=>{'use strict';
const API=window.VEXA_ACCOUNT_API_BASE||'https://api-vexaaccount.onrender.com';
const AUTH=/^#\/(login|signin|register|forgot-password|verify-email|reset-password|login-2fa)/;
const $=(s,r=document)=>r.querySelector(s); const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const notify=(m,t='info')=>window.vexaNotify?window.vexaNotify(m,t):console.log(`[${t}]`,m);
const token=()=>window.vexaAccountAuth?.getToken?.()||null;
async function api(path,opt={}){const headers={'Content-Type':'application/json',...(opt.headers||{})};const t=token();if(t&&!headers.Authorization)headers.Authorization='Bearer '+t;const r=await fetch(API+path,{credentials:'include',...opt,headers});const d=await r.json().catch(()=>({success:false,message:'The server returned an invalid response.'}));if(d?.token&&window.vexaAccountAuth?.saveToken)window.vexaAccountAuth.saveToken(String(d.token));if(!r.ok||d.success===false)throw Object.assign(new Error(d.message||`Request failed (${r.status})`),{auth:r.status===401});return d}
function auth(title,lead,body){const app=$('#app');if(app)app.innerHTML=`<main class="auth"><section class="auth-card glass"><img class="logo" src="./public/brand.svg" alt="VexaAccount"><p class="eyebrow">VEXA ACCOUNT</p><h1>${title}</h1><p class="lead">${lead}</p>${body}</section></main>`}
function login(){auth('Welcome back','Sign in to securely manage your Vexa identity.',`<form id="login" class="auth-form"><label class="field">Email<input name="email" type="email" autocomplete="email" required></label><label class="field">Password<input name="password" type="password" autocomplete="current-password" required></label><div class="auth-row"><a href="#/forgot-password">Forgot password?</a></div><button class="submit">Sign in</button></form><div class="links">Don't have an account? <a href="#/register">Create one</a></div>`);$('#login').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget),b=$('button',e.currentTarget);b.disabled=true;try{const d=await api('/api/auth/login',{method:'POST',body:JSON.stringify({email:String(f.get('email')).trim(),password:String(f.get('password'))})});if(d.requiresAuthenticator2fa||d.requiresEmail2fa){sessionStorage.setItem('vexa_login_2fa',JSON.stringify({type:d.requiresEmail2fa?'email':'authenticator',userId:d.userId,email:String(f.get('email')).trim()}));location.hash='#/login-2fa';return}notify('Signed in successfully.','success');location.hash='#/';window.dispatchEvent(new HashChangeEvent('hashchange'));}catch(x){notify(x.message,'error');b.disabled=false)}}}
function otp(type,userId,email){auth('Verify your sign-in',type==='email'?'Enter the code sent to your email.':'Enter the code from your authenticator app.',`<form id="otp" class="auth-form"><label class="field">Verification code<input name="otp" inputmode="numeric" autocomplete="one-time-code" maxlength="6" required></label><button class="submit">Verify and continue</button></form><div class="links"><a href="#/login">Cancel</a>${type==='email'?'<button id="resend" class="link-button" type="button">Resend code</button>':''}</div>`);$('#otp').onsubmit=async e=>{e.preventDefault();const code=String(new FormData(e.currentTarget).get('otp')).trim();try{const d=await api(type==='email'?'/api/auth/verify-email-2fa':'/api/auth/twofa/verify',{method:'POST',body:JSON.stringify(type==='email'?{userId,email,otp:code}:{userId,token:code})});if(d.token)window.vexaAccountAuth.saveToken(d.token);sessionStorage.removeItem('vexa_login_2fa');notify('Sign-in verified successfully.','success');location.hash='#/';window.dispatchEvent(new HashChangeEvent('hashchange'));}catch(x){notify(x.message,'error')}};if(type==='email')$('#resend').onclick=async()=>{try{await api('/api/auth/resend-email-2fa',{method:'POST',body:JSON.stringify({userId,email})});notify('A new verification code was sent.','info')}catch(x){notify(x.message,'error')}}}
function register(){auth('Create your VexaAccount','One secure identity for your connected Vexa services.',`<form id="register" class="auth-form"><label class="field">Full name<input name="name" autocomplete="name" required></label><label class="field">Email<input name="email" type="email" autocomplete="email" required></label><label class="field">Password<input name="password" type="password" autocomplete="new-password" minlength="8" required></label><label class="field">Confirm password<input name="confirm" type="password" autocomplete="new-password" minlength="8" required></label><button class="submit">Create account</button></form><div class="links">Already have an account? <a href="#/login">Sign in</a></div>`);$('#register').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget),p=String(f.get('password')),email=String(f.get('email')).trim().toLowerCase();if(p!==String(f.get('confirm')))return notify('Passwords do not match.','error');const b=$('button',e.currentTarget);b.disabled=true;try{await api('/api/auth/register',{method:'POST',body:JSON.stringify({name:String(f.get('name')).trim(),email,password:p})});notify('Account created. Verify your email to continue.','success');location.hash='#/verify-email?email='+encodeURIComponent(email)}catch(x){notify(x.message,'error');b.disabled=false}}}
function verify(email){auth('Verify your email','Enter the 6-digit code sent to your email.',`<form id="verify" class="auth-form"><label class="field">Email<input id="ve" name="email" type="email" value="${esc(email)}" required></label><label class="field">Verification code<input name="otp" inputmode="numeric" autocomplete="one-time-code" pattern="[0-9]{6}" maxlength="6" required></label><button class="submit">Verify email</button></form><div class="links"><button id="resend" class="link-button" type="button">Resend code</button><a href="#/login">Back to sign in</a></div>`);$('#verify').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget),b=$('button',e.currentTarget);b.disabled=true;try{const d=await api('/api/auth/verify-otp',{method:'POST',body:JSON.stringify({email:String(f.get('email')).trim(),otp:String(f.get('otp')).trim()})});if(d.token)window.vexaAccountAuth.saveToken(d.token);notify(d.message||'Email verified successfully.','success');location.hash='#/';window.dispatchEvent(new HashChangeEvent('hashchange'));}catch(x){notify(x.message,'error');b.disabled=false}};$('#resend').onclick=async()=>{try{await api('/api/auth/resend-otp',{method:'POST',body:JSON.stringify({email:String($('#ve').value).trim()})});notify('A new verification code was sent.','info')}catch(x){notify(x.message,'error')}}}
function forgot(){auth('Reset your password','Enter your email and we will send a secure reset link.',`<form id="forgot" class="auth-form"><label class="field">Email<input name="email" type="email" autocomplete="email" required></label><button class="submit">Send reset link</button></form><div class="links"><a href="#/login">Back to sign in</a></div>`);$('#forgot').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget),b=$('button',e.currentTarget);b.disabled=true;try{const d=await api('/api/auth/forgot-password',{method:'POST',body:JSON.stringify({email:String(f.get('email')).trim()})});notify(d.message||'If the email is registered, a reset link has been sent.','success')}catch(x){notify(x.message,'error')}finally{b.disabled=false}}}
function reset(tok){if(!tok)return forgot();auth('Choose a new password','Create a strong password with at least 8 characters.',`<form id="reset" class="auth-form"><label class="field">New password<input name="password" type="password" minlength="8" required></label><label class="field">Confirm password<input name="confirm" type="password" minlength="8" required></label><button class="submit">Reset password</button></form><div class="links"><a href="#/login">Back to sign in</a></div>`);$('#reset').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget),p=String(f.get('password'));if(p!==String(f.get('confirm')))return notify('Passwords do not match.','error');try{const d=await api('/api/auth/reset-password',{method:'POST',body:JSON.stringify({token:tok,newPassword:p})});notify(d.message||'Password reset successfully.','success');setTimeout(()=>location.hash='#/login',1000)}catch(x){notify(x.message,'error')}}}
function route(){const hash=location.hash||'';if(!AUTH.test(hash)){if(token())return;location.hash='#/login';return}const [raw,qraw]=hash.slice(2).split('?'),q=new URLSearchParams(qraw||'');if(raw==='login')login();else if(raw==='login-2fa'){let d={};try{d=JSON.parse(sessionStorage.getItem('vexa_login_2fa')||'{}')}catch{}if(d.userId)otp(d.type||'authenticator',d.userId,d.email||'');else location.hash='#/login'}else if(raw==='register')register();else if(raw==='verify-email')verify(q.get('email')||'');else if(raw==='forgot-password')forgot();else if(raw==='reset-password')reset(q.get('token')||'')}
window.addEventListener('hashchange',route);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',route);else route();
})();
