(()=>{
'use strict';
if(window.__VEXA_ACCOUNT_CENTER_FETCH_GUARD_V6__)return;
window.__VEXA_ACCOUNT_CENTER_FETCH_GUARD_V6__=true;
const nativeFetch=window.fetch.bind(window),inflight=new Map(),cache=new Map(),cooldown=new Map();
const ACCOUNT=/^https?:\/\/[^/]+\/api\/account(?:\/|$)/i;
const CACHE_MS=120000,STALE_MS=600000,RATE_LIMIT_COOLDOWN_MS=30000,AUTH_FAILURE_COOLDOWN_MS=15000,REQUEST_TIMEOUT_MS=8000,NETWORK_COOLDOWN_MS=12000;
const urlOf=input=>typeof input==='string'?input:(input?.url||'');
const authFailureUntil=new Map(),networkFailureUntil=new Map();
const methodOf=(input,init)=>String(init?.method||(typeof input!=='string'?input?.method:'')||'GET').toUpperCase();
const normalizeUrl=url=>{try{const u=new URL(url,location.href);u.hash='';return u.toString()}catch{return url}};
const keyOf=(method,url)=>`${method} ${normalizeUrl(url)}`;
async function snapshot(response){return{status:response.status,statusText:response.statusText,headers:[...response.headers.entries()],body:await response.clone().text()}}
const responseFrom=s=>new Response(s.body,{status:s.status,statusText:s.statusText,headers:s.headers});
function cachedResponse(key,allowStale=false){const item=cache.get(key);if(!item)return null;const age=Date.now()-item.at;if(age<=CACHE_MS||(allowStale&&age<=STALE_MS))return responseFrom(item);cache.delete(key);return null}
function invalidateFor(url){const path=(()=>{try{return new URL(url,location.href).pathname}catch{return url}})();const related=new Set();if(path.includes('/profile'))related.add('/api/account/profile');if(path.includes('/settings'))related.add('/api/account/settings');if(path.includes('/preferences'))related.add('/api/account/preferences');if(path.includes('/security')){related.add('/api/account/security');related.add('/api/account/security/events');}if(path.includes('/sessions'))related.add('/api/account/sessions');if(path.includes('/apps'))related.add('/api/account/apps');if(path.includes('/notifications'))related.add('/api/account/notifications');if(path.includes('/support'))related.add('/api/account/support/tickets');if(path.includes('/credits'))related.add('/api/account/credits');if(path.includes('/storage'))related.add('/api/account/storage');for(const key of cache.keys())for(const p of related)if(key.includes(p))cache.delete(key)}
window.fetch=async(input,init={})=>{const url=urlOf(input),method=methodOf(input,init);if(!ACCOUNT.test(url)){return nativeFetch(input,init)}if(method!=='GET'){invalidateFor(url);return nativeFetch(input,init)}const key=keyOf(method,url),now=Date.now(),fresh=cachedResponse(key);if(fresh)return fresh;if(inflight.has(key))return responseFrom(await inflight.get(key));const authUntil=authFailureUntil.get(key)||0;if(now<authUntil){const stale=cachedResponse(key,true);if(stale)return stale;return new Response(JSON.stringify({success:false,message:'Authentication session expired. Please sign in again.'}),{status:401,headers:{'Content-Type':'application/json','Cache-Control':'no-store'}})}const networkUntil=networkFailureUntil.get(key)||0;if(now<networkUntil){const stale=cachedResponse(key,true);if(stale)return stale;return new Response(JSON.stringify({success:false,message:'Account service is temporarily unreachable. Your changes will retry when the connection returns.'}),{status:503,headers:{'Content-Type':'application/json','Cache-Control':'no-store','Retry-After':String(Math.ceil((networkUntil-now)/1000))}})}const quietUntil=cooldown.get(key)||0;if(now<quietUntil){const stale=cachedResponse(key,true);if(stale)return stale;return new Response(JSON.stringify({success:false,message:'Account data is temporarily rate-limited. Please try again shortly.'}),{status:429,headers:{'Content-Type':'application/json','Cache-Control':'no-store','Retry-After':String(Math.ceil((quietUntil-now)/1000))}})}const promise=(async()=>{let timer=null,controller=null;try{let requestInit={...init};if(!init.signal){controller=new AbortController();requestInit.signal=controller.signal;timer=setTimeout(()=>controller.abort(new DOMException('Account request timed out','TimeoutError')),REQUEST_TIMEOUT_MS)}const response=await nativeFetch(input,requestInit),snap=await snapshot(response);if(response.ok){cache.set(key,{...snap,at:Date.now()});cooldown.delete(key);authFailureUntil.delete(key);networkFailureUntil.delete(key)}else if(response.status===401){authFailureUntil.set(key,Date.now()+AUTH_FAILURE_COOLDOWN_MS)}else if(response.status===429){cooldown.set(key,Date.now()+RATE_LIMIT_COOLDOWN_MS)}return snap}catch(error){networkFailureUntil.set(key,Date.now()+NETWORK_COOLDOWN_MS);const stale=cache.get(key);if(stale&&Date.now()-stale.at<=STALE_MS)return stale;return new Response(JSON.stringify({success:false,message:'Account service is temporarily unreachable. Please retry in a moment.'}),{status:503,headers:{'Content-Type':'application/json','Cache-Control':'no-store','Retry-After':String(NETWORK_COOLDOWN_MS/1000)}})}finally{if(timer)clearTimeout(timer)}})();inflight.set(key,promise);try{return responseFrom(await promise)}finally{inflight.delete(key)}};
})();
