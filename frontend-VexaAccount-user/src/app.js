const API_BASE=window.VEXA_ACCOUNT_API_BASE||'https://api-vexaaccount.onrender.com';
const api=(path,options={})=>fetch(API_BASE+path,{credentials:'include',...options,headers:{'Content-Type':'application/json',...(options.headers||{})}});
async function json(path,options){const r=await api(path,options);const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||`Request failed: ${r.status}`);return d;}
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let state={session:null,profile:null,security:null,prefs:null,credits:null,apps:[],sessions:[],events:[],storage:[],view:'overview'};
async function load(){
 const s=await json('/api/auth/session'); if(!s?.success){renderLogin();return;}
 state.session=s;
 const results=await Promise.allSettled([json('/api/account/profile'),json('/api/account/security'),json('/api/account/preferences'),json('/api/account/credits'),json('/api/account/storage'),json('/api/account/apps'),json('/api/account/sessions'),json('/api/account/security/events')]);
 const [p,sec,prefs,credits,storage,apps,sessions,events]=results.map(x=>x.status==='fulfilled'?x.value:{});
 state.profile=p.user||s.user;state.security=sec.security||{};state.prefs=prefs.preferences||{};state.credits=credits.balance||{};state.storage=storage.records||[];state.apps=apps.applications||[];state.sessions=sessions.sessions||[];state.events=events.events||[];render();
}
function renderLogin(){document.getElementById('app').innerHTML=`<main class="shell"><section class="card"><p class="eyebrow">VEXA ACCOUNT</p><h1>Your account, everywhere.</h1><p>Sign in to manage your profile, security, connected applications, sessions, storage and account preferences.</p><a class="button" href="${API_BASE}/auth/login-page">Sign in</a></section></main>`;}
function nav(){return [['overview','Overview'],['profile','Profile'],['security','Security'],['apps','Applications'],['sessions','Sessions'],['storage','Cloud storage'],['credits','Credits & coins'],['preferences','Preferences']].map(([id,label])=>`<button class="button secondary ${state.view===id?'active':''}" data-view="${id}">${label}</button>`).join('');}
function shell(content){const u=state.profile||state.session.user||{};document.getElementById('app').innerHTML=`<div class="shell"><header><div><p class="eyebrow">VEXA ACCOUNT</p><h1>Account Center</h1><p>${esc(u.email||'')} · ${esc(u.name||'Account')}</p></div><button id="logout" class="button secondary">Sign out</button></header><nav class="toolbar">${nav()}</nav><main class="grid">${content}</main></div>`;document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>{state.view=b.dataset.view;render();});document.getElementById('logout').onclick=async()=>{await api('/api/auth/logout',{method:'POST'});location.reload();};}
function card(title,body){return `<section class="card"><h2>${title}</h2>${body}</section>`;}
function overview(){const u=state.profile||{};return card('Account overview',`<p>Manage your VexaAccount identity and every connected VexaApp from one place.</p><div class="grid"><div class="card"><small>Connected applications</small><h2>${state.apps.length}</h2></div><div class="card"><small>Active sessions</small><h2>${state.sessions.length}</h2></div><div class="card"><small>Credit score</small><h2>${esc(state.credits.credit_score??0)}</h2></div><div class="card"><small>Coins</small><h2>${esc(state.credits.coins??0)}</h2></div></div><p><strong>${esc(u.email||'')}</strong><br>Account status: ${u.is_active?'Active':'Disabled'} · Verified: ${u.is_verified?'Yes':'No'}</p>`)+card('Recent security activity',(state.events.slice(0,8).map(e=>`<article class="row"><strong>${esc(e.event_type)}</strong><small>${esc(e.created_at)} · ${esc(e.ip_address||'')}</small></article>`).join('')||'<p>No recent security activity.</p>');}
function profile(){const u=state.profile||{};return card('Profile',`<form id="profileForm"><label>Name<input name="name" value="${esc(u.name)}"></label><label>First name<input name="first_name" value="${esc(u.first_name)}"></label><label>Last name<input name="last_name" value="${esc(u.last_name)}"></label><label>Country<input name="country" value="${esc(u.country)}"></label><label>Gender<input name="gender" value="${esc(u.gender)}"></label><label>Date of birth<input name="dob" type="date" value="${esc(u.dob?String(u.dob).slice(0,10):'')}"></label><button class="button" type="submit">Save profile</button></form><hr><h3>Change email</h3><form id="emailForm"><input name="email" type="email" value="${esc(u.email)}" required><input name="password" type="password" placeholder="Current password" required><button class="button" type="submit">Change email</button></form><hr><h3>Change password</h3><form id="passwordForm"><input name="currentPassword" type="password" placeholder="Current password" required><input name="newPassword" type="password" placeholder="New password (8+ characters)" required><button class="button" type="submit">Change password</button></form>`);document.getElementById('profileForm').onsubmit=async e=>{e.preventDefault();await json('/api/account/profile',{method:'PATCH',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});await load();};document.getElementById('emailForm').onsubmit=async e=>{e.preventDefault();const d=await json('/api/account/email',{method:'PATCH',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});alert(d.message);await load();};document.getElementById('passwordForm').onsubmit=async e=>{e.preventDefault();const d=await json('/api/account/password',{method:'PATCH',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});alert(d.message);await load();};}
function security(){return card('Security',`<p>Authenticator 2FA: <strong>${state.security.twofa_enabled?'Enabled':'Disabled'}</strong><br>Email 2FA: <strong>${state.security.email_2fa_enabled?'Enabled':'Disabled'}</strong><br>Security notifications: <strong>${state.security.security_notifications_enabled?'Enabled':'Disabled'}</strong></p><button id="disable2fa" class="button secondary">Disable 2FA</button> <button id="disablePasscode" class="button secondary">Disable passcode</button><h3>Active security events</h3>${state.events.map(e=>`<article class="row"><strong>${esc(e.event_type)}</strong><small>${esc(e.created_at)} · ${esc(e.ip_address||'')} · ${esc(e.user_agent||'')}</small></article>`).join('')||'<p>No events.</p>'}`);document.getElementById('disable2fa').onclick=async()=>{const password=prompt('Enter your password to disable 2FA');if(password){alert((await json('/api/account/security/disable-2fa',{method:'POST',body:JSON.stringify({password})})).message);await load();}};document.getElementById('disablePasscode').onclick=async()=>{if(confirm('Disable your account passcode?')){alert((await json('/api/account/security/disable-passcode',{method:'POST'})).message);await load();}};}
function apps(){return card('Connected applications',(state.apps.map(a=>`<article class="row"><div><strong>${esc(a.display_name||a.name||a.client_id)}</strong><small>${esc(a.client_id)} · ${esc(a.status||'active')}</small></div><button data-revoke="${esc(a.client_id)}" class="button secondary">Revoke access</button></article>`).join('')||'<p>No connected applications.</p>'));}
function sessions(){return card('Active sessions',(state.sessions.map(s=>`<article class="row"><div><strong>${esc(s.device||s.user_agent||'Browser session')}</strong><small>${esc(s.ip_address||'')} · ${esc(s.last_seen_at||s.created_at||'')}</small></div><button data-session="${esc(s.id)}" class="button secondary">Revoke</button></article>`).join('')||'<p>No active sessions.</p>');}
function storage(){return card('Cloud storage',`<p>VexaAccount-managed storage records linked to your account.</p>`+(state.storage.map(r=>`<article class="row"><div><strong>${esc(r.display_name||r.storage_key)}</strong><small>${esc(r.provider)} · ${esc(r.content_type||'')} · ${esc(r.size_bytes)} bytes · ${esc(r.status)}</small></div><button data-storage="${esc(r.id)}" class="button secondary">Remove record</button></article>`).join('')||'<p>No storage records.</p>'));}
function credits(){return card('Credits & coins',`<div class="grid"><div class="card"><small>Credit score</small><h2>${esc(state.credits.credit_score??0)}</h2></div><div class="card"><small>Coins</small><h2>${esc(state.credits.coins??0)}</h2></div></div>`);}
function preferences(){const p=state.prefs||{};return card('Preferences',`<form id="prefsForm"><label>Language<input name="locale" value="${esc(p.locale||'en')}"></label><label>Timezone<input name="timezone" value="${esc(p.timezone||'UTC')}"></label><label><input name="marketing_email_enabled" type="checkbox" ${p.marketing_email_enabled?'checked':''}> Marketing emails</label><label><input name="security_email_enabled" type="checkbox" ${p.security_email_enabled!==0?'checked':''}> Security emails</label><button class="button" type="submit">Save preferences</button></form><hr><h3>Delete account</h3><p>This permanently removes your VexaAccount data and revokes active sessions.</p><button id="deleteAccount" class="button secondary">Delete my account</button>`);document.getElementById('prefsForm').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target);await json('/api/account/preferences',{method:'PATCH',body:JSON.stringify({locale:f.get('locale'),timezone:f.get('timezone'),marketing_email_enabled:f.has('marketing_email_enabled'),security_email_enabled:f.has('security_email_enabled')})});alert('Preferences saved');await load();};document.getElementById('deleteAccount').onclick=async()=>{const password=prompt('Enter your password to permanently delete your account');if(password&&confirm('This cannot be undone. Continue?')){await json('/api/account/account',{method:'DELETE',body:JSON.stringify({password})});location.reload();}};}
function render(){let c={overview,profile,security,apps,sessions,storage,credits,preferences}[state.view]();shell(c);document.querySelectorAll('[data-revoke]').forEach(b=>b.onclick=async()=>{if(confirm('Revoke this application?')){await json(`/api/account/apps/${encodeURIComponent(b.dataset.revoke)}/consent`,{method:'DELETE'});await load();}});document.querySelectorAll('[data-session]').forEach(b=>b.onclick=async()=>{await json(`/api/account/sessions/${encodeURIComponent(b.dataset.session)}`,{method:'DELETE'});await load();});document.querySelectorAll('[data-storage]').forEach(b=>b.onclick=async()=>{if(confirm('Remove this storage record?')){await json(`/api/account/storage/${encodeURIComponent(b.dataset.storage)}`,{method:'DELETE'});await load();}});}
load().catch(e=>{document.getElementById('app').innerHTML=`<main class="shell"><section class="card"><h1>VexaAccount</h1><p>${esc(e.message)}</p><button class="button" onclick="location.reload()">Retry</button></section></main>`;});
