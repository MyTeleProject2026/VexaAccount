(()=>{'use strict';
if(window.__VEXA_SSO_FACTORY_V3__)return;window.__VEXA_SSO_FACTORY_V3__=true;
const API=(window.VEXA_ACCOUNT_ADMIN_API_BASE||'https://api-vexaaccount.onrender.com').replace(/\/$/,'');
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const api=async(path,opt={})=>{const r=await fetch(API+path,{credentials:'include',...opt,headers:{'Content-Type':'application/json',...(opt.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok||d.success===false)throw Error(d.message||d.error||`Request failed (${r.status})`);return d};
const copy=async(v)=>{try{await navigator.clipboard.writeText(v)}catch{const t=document.createElement('textarea');t.value=v;document.body.appendChild(t);t.select();document.execCommand('copy');t.remove()}};
const APPS={
 VexaMail:{key:'vexamail',description:'Secure mail identity, profile and notification integration.',scopes:['openid','profile','email','account','session','notifications']},
 VexaWallet:{key:'vexawallet',description:'Wallet identity and account ownership integration.',scopes:['openid','profile','email','account','session']},
 VexaCloud:{key:'vexacloud',description:'Cloud account identity and session integration.',scopes:['openid','profile','email','account','session']},
 'Vexa Password Manager':{key:'vexa-password-manager',description:'Password-manager identity and session integration.',scopes:['openid','profile','email','account','session']},
 VexaAuthenticator:{key:'vexaauthenticator',description:'Authenticator identity and security-session integration.',scopes:['openid','profile','email','account','session','notifications']},
 'VexaWholes Professional':{key:'vexawholes-professional',description:'Professional ecosystem identity and account integration.',scopes:['openid','profile','email','account','session','applications','notifications']}
};
const profileFor=name=>APPS[name]||{key:String(name||'app').toLowerCase().replace(/[^a-z0-9]+/g,'-'),description:'Registered VexaAccount ecosystem application.',scopes:[]};
function cfg(d,id){const raw=d?.environment?.VEXA_ACCOUNT_SSO_CONFIG||d?.VEXA_ACCOUNT_SSO_CONFIG||'';let p={};try{p=typeof raw==='string'?JSON.parse(raw):raw||{}}catch{}return{url:String(p.url||API).replace(/\/$/,''),clientId:p.clientId||id,redirectUri:p.redirectUri||'',scopes:Array.isArray(p.scopes)?p.scopes:[],timeoutMs:Number(p.timeoutMs)||10000}}
function backendNode(a,c){return `// ${a.display_name} — VexaAccount SSO\n// File: backend/src/auth/vexaaccount-sso.js\nconst crypto=require('crypto');\nconst config=JSON.parse(process.env.VEXA_ACCOUNT_SSO_CONFIG);\nconst clientId=process.env.VEXA_ACCOUNT_CLIENT_ID||config.clientId;\nconst clientSecret=process.env.VEXA_ACCOUNT_CLIENT_SECRET;\nif(!clientId||!clientSecret)throw new Error('VexaAccount SSO credentials missing');\nconst stateStore=new Map();\nfunction begin(){const state=crypto.randomBytes(32).toString('hex');stateStore.set(state,Date.now()+300000);const u=new URL(config.url+'/api/sso/authorize');u.searchParams.set('response_type','code');u.searchParams.set('client_id',clientId);u.searchParams.set('redirect_uri',config.redirectUri);u.searchParams.set('scope',config.scopes.join(' '));u.searchParams.set('state',state);return u.toString()}\nfunction consumeState(state){const exp=stateStore.get(state);stateStore.delete(state);return Boolean(exp&&exp>Date.now())}\nasync function exchange(code){const body=new URLSearchParams({grant_type:'authorization_code',code,client_id:clientId,client_secret:clientSecret,redirect_uri:config.redirectUri});const r=await fetch(config.url+'/api/sso/token',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body});const d=await r.json().catch(()=>({}));if(!r.ok||d.error)throw Error(d.error_description||d.error||'SSO token exchange failed');return d}\nasync function userInfo(token){const r=await fetch(config.url+'/api/sso/userinfo',{headers:{Authorization:'Bearer '+token}});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'SSO userinfo failed');return d}\nmodule.exports={begin,consumeState,exchange,userInfo,config};`}
function routeNode(a){return `// ${a.display_name} — File: backend/src/routes/auth-vexaaccount.js\nconst express=require('express');const {begin,consumeState,exchange,userInfo}=require('../auth/vexaaccount-sso');const router=express.Router();\nrouter.get('/vexaaccount',(_req,res)=>res.redirect(begin()));\nrouter.get('/vexaaccount/callback',async(req,res,next)=>{try{const {code,state,error}=req.query;if(error)return res.redirect('/login?error='+encodeURIComponent(error));if(!code||!state||!consumeState(state))return res.status(400).json({error:'INVALID_OR_EXPIRED_SSO_STATE'});const tokens=await exchange(code);const account=await userInfo(tokens.access_token);\n// APP-SPECIFIC ADAPTER: upsert by VexaAccount subject, preserve existing local profile, then create the local session.\nconst user=await req.app.locals.userRepository.upsertFromVexaAccount(account);req.session.userId=user.id;req.session.vexaAccountSubject=account.sub;res.redirect('/dashboard')}catch(e){next(e)}});\nrouter.post('/vexaaccount/logout',(req,res)=>req.session.destroy(()=>res.json({success:true})));module.exports=router;`}
function frontend(framework,a){const ext=framework==='vue'?'js':'jsx';return `// ${a.display_name} — File: frontend-user/src/auth/VexaAccountLogin.${ext}\n// Browser-safe only: no client secret, no token exchange.\nexport function signInWithVexaAccount(){window.location.assign('/api/auth/vexaaccount')}\nexport function VexaAccountLogin(){return {label:'Continue with VexaAccount',onClick:signInWithVexaAccount}}\n`}
function mapping(a){return `-- ${a.display_name} — File: database/vexaaccount-sso.sql\n-- Adapt user_id type to the target app. Keep VexaAccount subject unique and do not overwrite unrelated profile fields.\nCREATE TABLE IF NOT EXISTS vexaaccount_identities (id BIGINT PRIMARY KEY AUTO_INCREMENT,user_id BIGINT NOT NULL,provider VARCHAR(32) NOT NULL DEFAULT 'vexaaccount',subject_id VARCHAR(255) NOT NULL,email VARCHAR(320) NULL,profile_json JSON NULL,created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,UNIQUE KEY uq_vexa_subject(provider,subject_id),UNIQUE KEY uq_vexa_user(provider,user_id));`}
function files(a,c,framework,frontend){const node=framework==='nodeExpress'||framework==='nextjs';const arr=[];if(node){arr.push(['backend/src/auth/vexaaccount-sso.js',backendNode(a,c)],['backend/src/routes/auth-vexaaccount.js',routeNode(a)]);}else{arr.push(['backend/app/auth/vexaaccount_sso.py',`# ${a.display_name} — server-only VexaAccount SSO\nimport os,secrets,time,json,httpx\nconfig=json.loads(os.environ['VEXA_ACCOUNT_SSO_CONFIG']);client_id=os.getenv('VEXA_ACCOUNT_CLIENT_ID') or config['clientId'];client_secret=os.getenv('VEXA_ACCOUNT_CLIENT_SECRET')\n_states={}\ndef begin():\n state=secrets.token_urlsafe(32);_states[state]=time.time()+300;return state\nasync def exchange(code):\n async with httpx.AsyncClient(timeout=config.get('timeoutMs',10000)/1000) as c:\n  r=await c.post(config['url']+'/api/sso/token',data={'grant_type':'authorization_code','code':code,'client_id':client_id,'client_secret':client_secret,'redirect_uri':config['redirectUri']});r.raise_for_status();return r.json()`],['backend/app/routes/vexaaccount.py',`# ${a.display_name} — callback adapter\n# Validate server-side state, exchange code, fetch userinfo, upsert local identity, create local session.\n`]);}
arr.push(['backend/src/middleware/require-vexaaccount-user.js',`// ${a.display_name} — local session guard\nmodule.exports=(req,res,next)=>{if(!req.session?.userId)return res.status(401).json({error:'AUTHENTICATION_REQUIRED'});next()};`],['database/vexaaccount-sso.sql',mapping(a)],['frontend-user/src/services/vexaAccountSso.js',`// ${a.display_name} — browser-safe SSO client\nexport const signInWithVexaAccount=()=>window.location.assign('/api/auth/vexaaccount');\nexport const getSession=async()=>{const r=await fetch('/api/auth/session',{credentials:'include'});return r.ok?r.json():null};\nexport const signOut=async()=>{await fetch('/api/auth/vexaaccount/logout',{method:'POST',credentials:'include'});window.location.assign('/login')};`],['frontend-user/src/auth/VexaAccountLogin.'+(frontend==='Vue'?'js':'jsx'),frontend(framework,a)],['frontend-user/src/auth/VexaAccountAuthGuard.jsx',`// ${a.display_name} — protected UI boundary\nexport default function VexaAccountAuthGuard({children}){return children}`]);
return arr}
function tooling(a,c,framework,frontend,profile){const env=`VEXA_ACCOUNT_CLIENT_ID=${c.clientId}\nVEXA_ACCOUNT_CLIENT_SECRET=PASTE_ONE_TIME_SECRET_HERE\nVEXA_ACCOUNT_SSO_CONFIG=${JSON.stringify(c)}`;const test=`// File: tests/vexaaccount-sso.e2e.test.js\n// Generated E2E contract for ${a.display_name}.\n// Assert: authorization redirect -> exact registered callback -> server token exchange -> userinfo -> local identity upsert -> local session -> dashboard.\n// Negative cases: denied access, missing/expired state, invalid code, revoked/disabled client, token exchange failure, missing scope, logout.\n// Security assertion: client secret is backend-only and absent from browser bundles/URLs.\n`;const deploy=`# File: deployment/vexaaccount-sso.md\n# ${a.display_name} deployment contract\n1. Store VEXA_ACCOUNT_CLIENT_SECRET only in the backend secret manager.\n2. Set VEXA_ACCOUNT_CLIENT_ID and VEXA_ACCOUNT_SSO_CONFIG on the backend.\n3. Register the exact HTTPS redirect URI in VexaAccount.\n4. Run database/vexaaccount-sso.sql (adapt user_id type if required).\n5. Mount the generated auth route and local session middleware.\n6. Run the generated staging E2E tests.\n7. Enable production only when the registry client is active and callback/token exchange succeed.\n`;const config=JSON.stringify({application:a.display_name,applicationKey:profile.key,framework,frontend,description:profile.description,clientId:c.clientId,authorizationEndpoint:c.url+'/api/sso/authorize',tokenEndpoint:c.url+'/api/sso/token',userinfoEndpoint:c.url+'/api/sso/userinfo',redirectUri:c.redirectUri,scopes:c.scopes,timeoutMs:c.timeoutMs,profileScopes:profile.scopes},null,2);const manifest=JSON.stringify({format:'vexaaccount-sso-patch-v2',application:a.display_name,framework,frontend,operations:[...files(a,c,framework,frontend).map(x=>({type:'add',path:x[0]})),{type:'add',path:'backend/.env.example'},{type:'add',path:'tests/vexaaccount-sso.e2e.test.js'},{type:'add',path:'deployment/vexaaccount-sso.md'},{type:'add',path:'integration-config.json'}],patchPolicy:'Review and apply against the target repository; this browser generator never writes another repository automatically and never receives the client secret.',config:c},null,2);return [['backend/.env.example',env],['integration-config.json',config],['tests/vexaaccount-sso.e2e.test.js',test],['deployment/vexaaccount-sso.md',deploy],['integration-patch-manifest.json',manifest],['VEXAACCOUNT-SSO-SETUP.md',`# ${a.display_name} VexaAccount SSO\n\nFramework: ${framework}\nFrontend: ${frontend}\nScopes: ${c.scopes.join(', ')||'none'}\nRedirect: ${c.redirectUri||'NOT REGISTERED'}\n\nThis package contains source adapters, environment contract, database identity mapping, E2E verification contract and deployment checklist. The target app must connect its existing user repository/session implementation at the marked adapter point.\n`]]}
async function open(id){const[aRes,cRes]=await Promise.all([api('/api/sso-registry/applications/'+encodeURIComponent(id)),api('/api/sso-registry/applications/'+encodeURIComponent(id)+'/integration-config')]);const a=aRes.application||{};const c=cfg(cRes,id);const profile=profileFor(a.display_name);const m=document.createElement('div');m.className='os-modal';m.innerHTML=`<section class="os-modal-card os-generator-modal"><div class="os-modal-head"><div><p class="os-eyebrow">OWNER SSO INTEGRATION FACTORY · V3</p><h2>${esc(a.display_name||id)}</h2><p class="os-muted">${esc(profile.description)}</p></div><button class="os-btn" data-x>Close</button></div><div class="os-form-grid"><label class="os-field">Backend framework<select id="f-fw"><option value="nodeExpress">Node / Express</option><option value="nextjs">Next.js server integration</option><option value="fastapi">Python / FastAPI</option><option value="django">Python / Django adapter</option></select></label><label class="os-field">Frontend framework<select id="f-fe"><option>React</option><option>Next.js</option><option>Vue</option><option>Vanilla JS</option></select></label></div><section class="os-panel"><h3>Application-specific profile</h3><div class="os-list"><div class="os-row"><span>Application key</span><b>${esc(profile.key)}</b></div><div class="os-row"><span>Recommended scopes</span><b>${esc(profile.scopes.join(', '))}</b></div><div class="os-row"><span>Registered scopes</span><b>${esc(c.scopes.join(', '))}</b></div><div class="os-row"><span>Exact callback</span><b>${esc(c.redirectUri||'NOT REGISTERED')}</b></div></div></section><div class="os-actions"><button class="os-btn os-primary" data-build>Generate complete source package</button></div><div id="f-out"></div></section>`;document.body.appendChild(m);m.querySelector('[data-x]').onclick=()=>m.remove();m.querySelector('[data-build]').onclick=()=>{const fw=m.querySelector('#f-fw').value,fe=m.querySelector('#f-fe').value;const fs=[...files(a,c,fw,fe),...tooling(a,c,fw,fe,profile)];const out=m.querySelector('#f-out');out.innerHTML=`<section class="os-panel"><h3>${fs.length} generated files</h3><p class="os-muted">Each file is deterministic and based on the authoritative registry response. Copy individually or copy the complete source manifest.</p><div class="os-list">${fs.map((f,i)=>`<article class="os-card"><div class="os-card-main"><b>${esc(f[0])}</b><pre class="os-code os-code-block">${esc(f[1])}</pre></div><div class="os-actions"><button class="os-btn os-primary" data-c="${i}">Copy file</button></div></article>`).join('')}</div><div class="os-actions"><button class="os-btn" data-all>Copy complete package manifest</button><button class="os-btn" data-check>Validate package</button></div><p class="os-muted" data-result></p></section>`;out.querySelectorAll('[data-c]').forEach(b=>b.onclick=async()=>{await copy(fs[+b.dataset.c][1]);b.textContent='Copied ✓'});out.querySelector('[data-all]').onclick=async()=>copy(JSON.stringify(Object.fromEntries(fs),null,2));out.querySelector('[data-check]').onclick=()=>{const e=[];if(!c.clientId)e.push('client ID missing');if(!c.redirectUri)e.push('exact redirect URI missing');if(!c.scopes.length)e.push('scopes missing');if(!profile.scopes.every(s=>c.scopes.includes(s)))e.push('recommended app scopes are not all registered');out.querySelector('[data-result]').textContent=e.length?'Validation needs attention: '+e.join(' · '):'Package validation passed. Staging E2E verification is still required before production.'};window.__VEXA_SSO_PACKAGE__={application:a,profile,config:c,framework:fw,frontend:fe,files:fs}}}
function intercept(e){const b=e.target.closest?.('[data-gen-inline], [data-os-action^="sso-open:"]');if(!b)return;const id=(b.dataset.osAction||'').slice(9);if(!id)return;e.preventDefault();e.stopImmediatePropagation();open(id).catch(x=>alert(x.message))}
document.addEventListener('click',intercept,true);window.vexaSsoFactoryV3={open};
})();