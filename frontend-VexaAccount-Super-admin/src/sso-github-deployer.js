(()=>{'use strict';
if(window.__VEXA_SSO_GITHUB_DEPLOYER__)return;window.__VEXA_SSO_GITHUB_DEPLOYER__=true;
const API=(window.VEXA_ACCOUNT_ADMIN_API_BASE||'https://api-vexaaccount.onrender.com').replace(/\/$/,'');
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const api=async(path,opt={})=>{const r=await fetch(API+path,{credentials:'include',...opt,headers:{'Content-Type':'application/json',...(opt.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok||d.success===false)throw Error(d.message||d.error||`Request failed (${r.status})`);return d};
function add(){document.querySelectorAll('.os-generator-modal').forEach(modal=>{if(modal.querySelector('[data-github-deploy]'))return;const files=window.__VEXA_LAST_GENERATED_FILES__;if(!Array.isArray(files)||!files.length)return;const head=modal.querySelector('.os-modal-head');if(!head)return;const box=document.createElement('div');box.className='os-panel';box.style.marginTop='16px';box.innerHTML='<p class="os-eyebrow">OPTIONAL REPOSITORY INSTALLATION</p><h3>Commit generated integration to GitHub</h3><p class="os-muted">The Owner backend performs the Git operation server-side. The browser never receives a GitHub token and no client secret is included in the generated files.</p><div class="os-form-grid"><label class="os-field">Target repository<input data-gh-repo placeholder="MyTeleProject2026/VexaMail" autocomplete="off"></label><label class="os-field">Branch<input data-gh-branch value="main" autocomplete="off"></label><label class="os-field full">Path prefix<input data-gh-prefix placeholder="Optional, e.g. vexaaccount-sso"></label><label class="os-field full">Commit message<input data-gh-message value="feat(auth): install VexaAccount SSO integration"></label></div><div class="os-actions"><button type="button" class="os-btn" data-gh-preflight>Check repository</button><button type="button" class="os-btn os-primary" data-github-deploy>Commit integration</button></div><p class="os-muted" data-gh-result></p>';head.insertAdjacentElement('afterend',box);const result=box.querySelector('[data-gh-result]');box.querySelector('[data-gh-preflight]').onclick=async()=>{result.textContent='Checking…';try{const d=await api('/api/sso-integration/status');if(!d.configured)throw Error('GitHub deployment is not configured on the VexaAccount backend.');const p=await api('/api/sso-integration/preflight',{method:'POST',body:JSON.stringify({repository:box.querySelector('[data-gh-repo]').value.trim()})});result.textContent=`Ready: ${p.repository.repository} · default branch ${p.repository.defaultBranch||'unknown'}.`;}catch(e){result.textContent=e.message;}};box.querySelector('[data-github-deploy]').onclick=async()=>{const repo=box.querySelector('[data-gh-repo]').value.trim(),branch=box.querySelector('[data-gh-branch]').value.trim()||'main',prefix=box.querySelector('[data-gh-prefix]').value.trim(),message=box.querySelector('[data-gh-message]').value.trim();if(!repo)return result.textContent='Enter a target repository.';if(!confirm(`Commit ${files.length} generated SSO files to ${repo}/${branch}?`))return;result.textContent='Committing integration…';try{const p=await api('/api/sso-integration/preflight',{method:'POST',body:JSON.stringify({repository:repo})});if(!p.repository.permissions?.push&&!p.repository.permissions?.admin&&!p.repository.permissions?.maintain)throw Error('GitHub token does not have push permission for this repository.');const d=await api('/api/sso-integration/deploy',{method:'POST',body:JSON.stringify({repository:repo,branch,pathPrefix:prefix,commitMessage:message,files})});result.innerHTML=`Committed <b>${esc(d.deployment.commitSha)}</b> · <a href="${esc(d.deployment.commitUrl)}" target="_blank" rel="noopener">View commit</a>`;}catch(e){result.textContent=e.message;}}});}
new MutationObserver(add).observe(document.documentElement,{subtree:true,childList:true});setInterval(add,1000);})();
