(()=>{'use strict';
function install(){const b=document.getElementById('downloadReceipt');if(!b||b.dataset.safePdf==='1')return;b.dataset.safePdf='1';b.onclick=()=>{const d=window.__VEXA_SSO_STATE__?.lastCreated;const source=d||(()=>{try{return state.lastCreated}catch{return null}})();if(!source){alert('No integration receipt is available.');return}if(!window.jspdf?.jsPDF){alert('PDF generator is still loading. Please wait a moment.');return}const {jsPDF}=window.jspdf;const doc=new jsPDF({unit:'mm',format:'a4'});doc.setFillColor(7,10,18);doc.rect(0,0,210,297,'F');doc.setFillColor(124,58,237);doc.roundedRect(15,15,180,30,5,5,'F');doc.setTextColor(255,255,255);doc.setFontSize(24);doc.text('VexaAccount',24,31);doc.setFontSize(9);doc.text('CENTRAL IDENTITY PLATFORM • INTEGRATION RECEIPT',24,38);let y=58;const rows=[['APPLICATION NAME',source.name],['APPLICATION KEY',source.key],['REDIRECT URL',source.redirects],['CLIENT ID',source.clientId],['GRANTED SCOPES',source.scopes],['VEXA ACCOUNT SSO URL','https://api-vexaaccount.onrender.com']];rows.forEach(([label,value])=>{doc.setFillColor(18,25,42);doc.roundedRect(15,y,180,22,3,3,'F');doc.setTextColor(125,211,252);doc.setFontSize(7);doc.text(label,20,y+7);doc.setTextColor(245,248,255);doc.setFontSize(8);doc.text(doc.splitTextToSize(String(value||''),165),20,y+13);y+=28});doc.setFillColor(17,24,39);doc.roundedRect(15,y,180,42,4,4,'F');doc.setTextColor(125,211,252);doc.setFontSize(8);doc.text('RENDER BACKEND ENVIRONMENT VARIABLES',20,y+8);doc.setTextColor(245,248,255);doc.setFontSize(7);const cfg=JSON.stringify({url:'https://api-vexaaccount.onrender.com',clientId:source.clientId,redirectUri:String(source.redirects||'').split(', ')[0]||'',scopes:String(source.scopes||'').split(/\s+/).filter(Boolean),timeoutMs:10000});doc.text(doc.splitTextToSize('VEXA_ACCOUNT_SSO_CONFIG='+cfg,165),20,y+16);doc.text('VEXA_ACCOUNT_CLIENT_SECRET=<use the secret shown once at creation/rotation>',20,y+32);y+=52;doc.setTextColor(170,180,200);doc.setFontSize(8);doc.text('Security: the client secret is intentionally not embedded in VEXA_ACCOUNT_SSO_CONFIG.',15,y);doc.save((source.key||'vexaaccount-application')+'-integration-receipt.pdf')};}
new MutationObserver(install).observe(document.documentElement,{childList:true,subtree:true});window.addEventListener('load',()=>setTimeout(install,300));
})();
