(()=>{
'use strict';
if(window.__VEXA_OWNER_OPERATION_CENTER_UI_V3__)return;
window.__VEXA_OWNER_OPERATION_CENTER_UI_V3__=true;
const API=(window.VEXA_ACCOUNT_ADMIN_API_BASE||'https://api-vexaaccount.onrender.com').replace(/\/$/,'');
const TERMINAL=new Set(['completed','failed','cancelled','stalled']);
const ACTIVE_POLL_MS=5000,IDLE_POLL_MS=30000,AUTH_RETRY_MS=2500;
let open=false,pollTimer=null,pollInFlight=false,selected='',hasLoaded=false,operations=new Map(),authReady=false,authRetryTimer=null;
const $=(s,r=document)=>r.querySelector(s);
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const ownerReady=()=>{if(authReady)return true;const state=String(window.__VEXA_OWNER_BOOT_STATE__||'').toLowerCase();return ['ready','authenticated','rendered-gateway'].includes(state)};
const api=async(path,opt={})=>{const ctl=new AbortController();const timer=setTimeout(()=>ctl.abort(),6000);try{const r=await fetch(API+path,{credentials:'include',cache:'no-store',...opt,signal:ctl.signal,headers:{Accept:'application/json',...(opt.headers||{})}});const d=await r.json().catch(()=>({}));if(r.status===401||r.status===403){const e=Error('Owner session is not authenticated');e.code='AUTH_REQUIRED';throw e}if(!r.ok||d.success===false)throw Error(d.message||d.error||`Request failed (${r.status})`);return d}finally{clearTimeout(timer)}};
const formatElapsed=o=>{const start=Date.parse(o.startedAt||o.createdAt),end=Date.parse(o.completedAt||new Date().toISOString());if(!Number.isFinite(start))return '—';let n=Math.max(0,Math.floor((end-start)/1000));const h=Math.floor(n/3600),m=Math.floor((n%3600)/60),s=n%60;return [h,m,s].map(v=>String(v).padStart(2,'0')).join(':')};
const ensure=()=>{let root=$('#vexa-owner-operation-center');if(root)return root;root=document.createElement('section');root.id='vexa-owner-operation-center';root.setAttribute('aria-hidden','true');root.innerHTML=`<div class="voc-panel" role="dialog" aria-label="Owner background operations"><header class="voc-head"><div><span class="voc-kicker">OWNER OS · BACKGROUND TASKS</span><h2>Operation Center</h2><small>Server-side jobs continue while you navigate Owner OS.</small></div><button type="button" class="voc-close" data-close aria-label="Close Operation Center">×</button></header><div class="voc-toolbar"><span data-count>0 operations</span><button type="button" data-refresh>Refresh</button></div><div class="voc-list" data-list><div class="voc-empty">Waiting for Owner session…</div></div></div>`;document.body.appendChild(root);root.addEventListener('click',onClick);return root};
const button=()=>{if($('#vexa-operation-center-trigger'))return;const b=document.createElement('button');b.id='vexa-operation-center-trigger';b.type='button';b.title='Background operations';b.setAttribute('aria-label','Open background operations');b.innerHTML='<span class="voc-trigger-dot"></span><span>Operations</span>';b.onclick=()=>{open=!open;renderVisibility();if(open)refresh(true)};document.body.appendChild(b)};
const renderVisibility=()=>{const r=ensure();r.classList.toggle('visible',open);r.setAttribute('aria-hidden',open?'false':'true');document.documentElement.classList.toggle('voc-open',open)};
const statusClass=s=>{s=String(s||'queued').toLowerCase();return s==='completed'?'ok':TERMINAL.has(s)?'bad':'live'};
const card=o=>{const terminal=TERMINAL.has(String(o.status).toLowerCase());const events=(o.events||[]).slice(-5).reverse();return `<article class="voc-card ${selected===o.id?'selected':''}"><div class="voc-main" data-select="${esc(o.id)}"><div class="voc-title"><span class="voc-status ${statusClass(o.status)}"></span><b>${esc(o.label||o.type||'Owner operation')}</b><span class="voc-state">${esc(o.status||'queued')}</span></div><div class="voc-phase">${esc(o.phase||'QUEUED')} · ${Math.max(0,Math.min(100,Number(o.progress)||0))}% · ${formatElapsed(o)}</div><div class="voc-detail">${esc(o.detail||'Working…')}</div><div class="voc-bar"><i style="width:${Math.max(0,Math.min(100,Number(o.progress)||0))}%"></i></div></div><div class="voc-actions">${!terminal?`<button type="button" data-cancel="${esc(o.id)}">Cancel</button>`:''}<button type="button" data-open="${esc(o.id)}">${selected===o.id?'Hide log':'View log'}</button></div>${selected===o.id?`<div class="voc-log">${events.map(e=>`<div><time>${esc(new Date(e.at).toLocaleTimeString())}</time><b>${esc(e.phase)}</b><span>${esc(e.detail)}</span></div>`).join('')||'<small>No events yet.</small>'}</div>`:''}</article>`};
const renderList=()=>{const r=ensure(),list=r.querySelector('[data-list]'),count=r.querySelector('[data-count]'),ops=[...operations.values()].sort((a,b)=>Date.parse(b.createdAt)-Date.parse(a.createdAt));count.textContent=`${ops.length} operation${ops.length===1?'':'s'}`;list.innerHTML=ops.length?ops.map(card).join(''):'<div class="voc-empty">No background operations are currently visible.</div>';};
const renderAuthWait=()=>{const r=ensure();r.querySelector('[data-count]').textContent='—';r.querySelector('[data-list]').innerHTML='<div class="voc-empty">Owner session required.<br><button type="button" data-signin style="margin-top:12px">Open Owner sign-in</button></div>';};
const schedule=()=>{clearTimeout(pollTimer);if(!open)return;const active=[...operations.values()].some(o=>!TERMINAL.has(String(o.status||'').toLowerCase()));pollTimer=setTimeout(()=>refresh(false),active?ACTIVE_POLL_MS:IDLE_POLL_MS)};
const scheduleAuthRetry=()=>{clearTimeout(authRetryTimer);if(!open)return;authRetryTimer=setTimeout(()=>refresh(false),AUTH_RETRY_MS)};
async function refresh(forceFull=false){if(pollInFlight)return;if(!ownerReady()){renderAuthWait();scheduleAuthRetry();return}pollInFlight=true;try{const path=forceFull||!hasLoaded?'/api/sso-application-analyzer/operations?includeTerminal=true&limit=50':'/api/sso-application-analyzer/operations?includeTerminal=false&limit=50';const d=await api(path);authReady=true;const incoming=d.operations||[];for(const o of incoming)operations.set(o.id,o);if(forceFull||!hasLoaded)hasLoaded=true;renderList()}catch(e){if(e?.code==='AUTH_REQUIRED'){authReady=false;renderAuthWait();scheduleAuthRetry()}else if(!hasLoaded&&open)renderList()}finally{pollInFlight=false;schedule()}}
async function loadSelected(id){if(!ownerReady()){renderAuthWait();scheduleAuthRetry();return}try{const d=await api('/api/sso-application-analyzer/operations/'+encodeURIComponent(id));if(d.operation)operations.set(id,d.operation);renderList()}catch(e){if(e?.code==='AUTH_REQUIRED'){authReady=false;renderAuthWait();scheduleAuthRetry()}}}
async function cancel(id){try{await api('/api/sso-application-analyzer/operations/'+encodeURIComponent(id)+'/cancel',{method:'POST'});selected=id;await loadSelected(id);await refresh(false)}catch(e){if(e?.code==='AUTH_REQUIRED'){authReady=false;renderAuthWait();scheduleAuthRetry();return}alert(e.message||'Unable to cancel operation')}}
function close(){open=false;renderVisibility();clearTimeout(pollTimer);clearTimeout(authRetryTimer);selected='';}
function openOwnerSignIn(){
 close();
 window.__VEXA_OWNER_BOOTING__=false;
 window.__VEXA_OWNER_BOOT_STATE__='unauthenticated';
 window.dispatchEvent(new CustomEvent('vexa-owner-session-lost',{detail:{source:'operation-center-signin'}}));
 const app=document.querySelector('#app');
 if(!app)return;
 app.innerHTML=`<main class="os-login"><section class="os-login-card"><span class="os-mark">V</span><p class="os-eyebrow">VEXAACCOUNT ECOSYSTEM</p><h1>Owner Access</h1><p class="os-muted">Secure authentication for the Owner OS.</p><p class="os-error" id="voc-login-error" hidden></p><form class="os-form" id="voc-owner-login"><label>Email<input id="voc-login-email" type="email" autocomplete="username" required></label><label>Password<input id="voc-login-password" type="password" autocomplete="current-password" required></label><button class="os-btn os-primary" type="submit">Enter Owner OS</button></form></section></main>`;
 const form=$('#voc-owner-login'),btn=form?.querySelector('button'),err=$('#voc-login-error');
 form?.addEventListener('submit',async e=>{e.preventDefault();if(!btn)return;btn.disabled=true;if(err)err.hidden=true;try{const d=await api('/api/auth/super-admin/login',{method:'POST',body:JSON.stringify({email:$('#voc-login-email').value.trim(),password:$('#voc-login-password').value})});if(!d?.success)throw Error(d?.message||'Owner sign-in failed');window.__VEXA_OWNER_BOOTING__=false;window.__VEXA_OWNER_BOOT_STATE__='signed-in';if(window.vexaOwnerOS?.reload){await window.vexaOwnerOS.reload()}else{window.location.reload()}}catch(x){if(err){err.textContent=x?.message||'Owner sign-in failed';err.hidden=false}btn.disabled=false}});
 setTimeout(()=>$('#voc-login-email')?.focus(),0);
}
function onClick(e){if(e.target.closest('[data-close]')){e.preventDefault();e.stopPropagation();close();return}if(e.target.closest('[data-signin]')){e.preventDefault();e.stopPropagation();openOwnerSignIn();return}if(e.target.closest('[data-refresh]')){refresh(true);return}const c=e.target.closest('[data-cancel]');if(c){cancel(c.dataset.cancel);return}const o=e.target.closest('[data-open]');if(o){selected=selected===o.dataset.open?'':o.dataset.open;if(selected)loadSelected(selected);else renderList();return}const s=e.target.closest('[data-select]');if(s){selected=selected===s.dataset.select?'':s.dataset.select;if(selected)loadSelected(selected);else renderList()}}
button();ensure();
window.addEventListener('vexa-owner-auth-ready',()=>{authReady=true;if(open)refresh(true)},{passive:true});
window.addEventListener('vexa-owner-session-lost',()=>{authReady=false;if(open)renderAuthWait()},{passive:true});
window.addEventListener('vexa-owner-operation-changed',()=>{if(open)refresh(false)},{passive:true});
window.addEventListener('keydown',e=>{if(e.key==='Escape'&&open)close()},{passive:true});
})();
