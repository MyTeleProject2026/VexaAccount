(()=>{
'use strict';
if(window.__VEXA_OWNER_ANALYZER_RESILIENCE__)return;
window.__VEXA_OWNER_ANALYZER_RESILIENCE__=true;
const API=(window.VEXA_ACCOUNT_ADMIN_API_BASE||'https://api-vexaaccount.onrender.com').replace(/\/$/,'');
const STORE='vexa.owner.async.planner.v2';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const terminal=s=>['completed','failed','cancelled','stalled'].includes(String(s||'').toLowerCase());
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
async function get(path){const r=await fetch(API+path,{credentials:'include',cache:'no-store',headers:{Accept:'application/json','Cache-Control':'no-cache'}});const d=await r.json().catch(()=>({}));if(!r.ok||d.success===false)throw Error(d.message||d.error||`Request failed (${r.status})`);return d}
function root(){return document.getElementById('vexa-async-planner')}
function setText(sel,text){const r=root();const e=r?.querySelector(sel);if(e)e.textContent=text}
function setStatus(status,phase,progress,detail){const r=root();if(!r)return;const s=r.querySelector('[data-status]'),p=r.querySelector('[data-percent]'),b=r.querySelector('[data-bar]'),d=r.querySelector('[data-phase]'),dot=r.querySelector('[data-dot]');if(s)s.textContent=String(status||'RUNNING').toUpperCase();if(p)p.textContent=`${Math.max(0,Math.min(100,Number(progress)||0))}%`;if(b)b.style.width=p.textContent;if(d)d.textContent=detail||phase||'';if(dot)dot.className='vap-dot '+(String(status).toLowerCase()==='completed'?'ok':terminal(status)?'err':'live')}
function save(op,repo,branch){try{localStorage.setItem(STORE,JSON.stringify({opId:op.id,kind:'analyze',startedAt:Date.parse(op.startedAt||'')||Date.now(),repository:repo,branch:branch,applicationKey:''}))}catch(_) {}}
function clear(){try{localStorage.removeItem(STORE)}catch(_) {}}
async function findActiveAnalysis(){for(let i=0;i<6;i++){try{const d=await get('/api/sso-application-analyzer/operations?includeTerminal=false&limit=10');const op=(d.operations||[]).find(o=>String(o.type)==='repository-analysis'&&!terminal(o.status));if(op)return op}catch(_){}await sleep(500)}return null}
async function watch(id,repo,branch){let delay=900,started=Date.now();while(Date.now()-started<30*60*1000){try{const d=await get('/api/sso-application-analyzer/operations/'+encodeURIComponent(id));const op=d.operation||d;save(op,repo,branch);setText('[data-op]',op.id||id);setStatus(op.status,op.phase,op.progress,op.detail);const elapsed=root()?.querySelector('[data-elapsed]');if(elapsed)elapsed.textContent=new Date(Math.max(0,Date.now()-(Date.parse(op.startedAt||'')||started))).toISOString().slice(11,19);if(terminal(op.status)){clear();if(op.status==='completed'){window.__VEXA_LAST_SOURCE_ANALYSIS__=op.result||{};const p=root()?.querySelector('[data-plan]');if(p)p.disabled=!(op.result?.files||[]).length;setText('[data-phase]','Analysis completed. Review the findings before continuing.')}else{root()?.querySelector('[data-output]')?.insertAdjacentHTML('afterbegin',`<div class="vap-error">Analysis ${esc(op.status)}: ${esc(op.error||op.detail||'No additional error information')}</div>`)}return op}delay=op.lastHeartbeatAt?1000:1800}catch(e){setStatus('running','NETWORK RETRY',0,'Temporary connection interruption; server operation continues independently.');delay=Math.min(4000,delay+500)}await sleep(delay)}setStatus('running','STILL RUNNING',0,'The server operation is still independent; this page will keep retrying without aborting it.');return null}
async function startNoAbort(){const r=root();if(!r)return;const repo=r.querySelector('[data-repo]')?.value.trim()||'';const branch=r.querySelector('[data-branch]')?.value.trim()||'main';if(!repo){setStatus('failed','INPUT',0,'Enter the target GitHub repository.');return}const button=r.querySelector('[data-analyze]');if(button)button.disabled=true;setStatus('queued','SUBMITTING',0,'Submitting analysis to the server worker…');r.querySelector('[data-output]').innerHTML='<div class="vap-ok">Analysis runs independently on the server. This submission has no short browser abort timer and cannot freeze the Owner page.</div>';try{const response=await fetch(API+'/api/sso-application-analyzer/analyze/async',{method:'POST',credentials:'include',cache:'no-store',headers:{Accept:'application/json','Content-Type':'application/json','Cache-Control':'no-cache','Pragma':'no-cache'},body:JSON.stringify({repository:repo,branch})});const d=await response.json().catch(()=>({}));if(response.ok&&d.success&&d.operation?.id){save(d.operation,repo,branch);setStatus(d.operation.status,d.operation.phase,d.operation.progress,d.operation.detail||'Server operation accepted.');await watch(d.operation.id,repo,branch);return}const recovered=await findActiveAnalysis();if(recovered?.id){save(recovered,repo,branch);setStatus(recovered.status,recovered.phase,recovered.progress,'Recovered active server operation after the submission connection closed.');await watch(recovered.id,repo,branch);return}throw Error(d.message||d.error||`Analysis submission failed (${response.status})`)}catch(e){const recovered=await findActiveAnalysis();if(recovered?.id){save(recovered,repo,branch);setStatus(recovered.status,recovered.phase,recovered.progress,'The request connection closed, but the durable server operation is still running.');await watch(recovered.id,repo,branch);return}setStatus('failed','SUBMISSION ERROR',0,e?.message||'Analysis submission failed');r.querySelector('[data-output]').insertAdjacentHTML('afterbegin',`<div class="vap-error">${esc(e?.message||'Analysis submission failed')}</div>`)}finally{if(button)button.disabled=false}}
function init(){document.addEventListener('click',e=>{const b=e.target?.closest?.('#vexa-async-planner [data-analyze]');if(!b)return;e.preventDefault();e.stopImmediatePropagation();void startNoAbort()},true)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();
