(()=>{'use strict';if(window.__VEXA_APPLICATION_PLANNER_UI__)return;window.__VEXA_APPLICATION_PLANNER_UI__=true;const API=(window.VEXA_ACCOUNT_ADMIN_API_BASE||'https://api-vexaaccount.onrender.com').replace(/\/$/,'');const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const $=(s,r=document)=>r.querySelector(s);const api=async(p,o={})=>{const r=await fetch(API+p,{credentials:'include',...o,headers:{'Content-Type':'application/json',...(o.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.message||d.error||`Request failed (${r.status})`);return d};const open=()=>{if($('#vexa-planner-overlay'))return;document.body.insertAdjacentHTML('beforeend',`<div id="vexa-planner-overlay" class="kit-overlay"><section class="kit-modal"><header><div><p>OWNER SOURCE REVIEW PLANNER</p><h2>Inspect exact files before integration</h2><span>This planner is read-only. It records the current GitHub blob SHA, source fingerprint, authentication anchors and proposed integration action.</span></div><button id="planner-close">×</button></header><div class="kit-grid"><label>GitHub repository<input id="planner-repo" placeholder="owner/repository" required></label><label>Repository branch<input id="planner-branch" value="main"></label><label>Application key<input id="planner-key" placeholder="my-app"></label></div><div class="kit-actions"><button id="planner-run" class="os-btn os-primary">Build precise source plan</button></div><div class="kit-note"><b>No target repository files are changed.</b> Any future patch/replacement must verify the planned blob SHA still matches and requires explicit Owner approval.</div><div id="planner-output"></div></section></div>`);$('#planner-close').onclick=close;$('#planner-run').onclick=run};const close=()=>$('#vexa-planner-overlay')?.remove();async function run(){const repo=$('#planner-repo').value.trim(),branch=$('#planner-branch').value.trim()||'main',key=$('#planner-key').value.trim()||repo.split('/').pop()||'application',out=$('#planner-output'),btn=$('#planner-run');if(!repo){out.innerHTML='<p class="kit-error">Enter the target GitHub repository.</p>';return}btn.disabled=true;out.innerHTML='<p>Reading selected source candidates and calculating review fingerprints…</p>';try{const d=await api('/api/sso-application-analyzer/plan',{method:'POST',body:JSON.stringify({repository:repo,branch,applicationKey:key})});window.__VEXA_LAST_SOURCE_PLAN__=d;const files=d.files||d.selectedFiles||[],policy=d.replacementPolicy||d.installationGuard||{};out.innerHTML=`<div class="kit-success"><b>Precise source plan completed.</b><span>${esc(d.repository?.name||repo)} · ${esc(d.repository?.branch||branch)} · ${files.length} reviewed files</span></div><div class="kit-analysis-grid"><article><b>Strategy</b><span>${esc(d.strategy||'additive-first')}</span></article><article><b>Files</b><span>${files.length}</span></article><article><b>Replacement</b><span>Owner approval required</span></article><article><b>Write access</b><span>Not used</span></article></div><details open><summary>Exact source review</summary><div class="kit-files">${files.map((f,i)=>`<article><div><b>${esc(f.path)}</b><small>action: ${esc(f.action||'review')} · confidence: ${esc(f.confidence||'unknown')}</small><small>blob SHA: ${esc(f.blobSha||f.sha||'not supplied')}</small><small>source SHA-256: ${esc(f.sourceSha256||f.sourceFingerprint||'not supplied')}</small></div><div><button class="kit-preview planner-file" data-i="${i}">Inspect</button></div></article>`).join('')||'<p>No candidate files were returned; review the analyzer output before proceeding.</p>'}</div></details><details><summary>Authentication and integration anchors</summary><pre>${esc(files.map(f=>`${f.path}\n${(f.anchors||[]).map(a=>`  • ${a.type||a.kind||'anchor'} @ ${a.line||a.lineNumber||'?'}${a.detail?` — ${a.detail}`:''}`).join('\n')}`).join('\n\n'))}</pre></details><details><summary>Installation guard</summary><pre>${esc(typeof policy==='string'?policy:JSON.stringify(policy,null,2))}</pre></details><details><summary>Warnings</summary><ul>${(d.warnings||[]).map(w=>`<li>${esc(w)}</li>`).join('')||'<li>Read-only planning only; no target files were modified.</li>'}</ul></details><div class="kit-note"><b>Owner approval gate:</b> Do not replace or patch an existing target authentication file unless its current blob SHA exactly matches this plan and the Owner explicitly approves that operation. If the SHA changes, create a new read-only plan.</div>`;out.querySelectorAll('.planner-file').forEach(b=>b.onclick=()=>{const f=files[Number(b.dataset.i)],p=document.createElement('pre');p.className='planner-inspect';p.textContent=JSON.stringify(f,null,2);out.appendChild(p);p.scrollIntoView({behavior:'smooth',block:'nearest'})})}catch(e){out.innerHTML=`<p class="kit-error">${esc(e.message)}</p>`}finally{btn.disabled=false}}function inject(){if(document.querySelector('[data-planner-launch]'))return;const host=document.querySelector('.os-actions')||document.querySelector('.os-controller-grid');if(!host)return;const b=document.createElement('button');b.className='os-btn';b.dataset.plannerLaunch='1';b.textContent='Build Source Review Plan';b.onclick=open;host.appendChild(b)}new MutationObserver(inject).observe(document.body,{childList:true,subtree:true});window.addEventListener('load',inject);setInterval(inject,1500)})();
