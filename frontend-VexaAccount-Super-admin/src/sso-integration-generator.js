(()=>{'use strict';
if(window.__VEXA_SSO_GENERATOR_V2__)return;window.__VEXA_SSO_GENERATOR_V2__=true;
const API=(window.VEXA_ACCOUNT_ADMIN_API_BASE||'https://api-vexaaccount.onrender.com').replace(/\/$/,'');
const $=(s,r=document)=>r.querySelector(s);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const api=async(path,opt={})=>{const r=await fetch(API+path,{credentials:'include',...opt,headers:{'Content-Type':'application/json',...(opt.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok||d.success===false)throw Error(d.message||d.error||`Request failed (${r.status})`);return d};
const copy=async(v)=>{try{await navigator.clipboard.writeText(v)}catch{const t=document.createElement('textarea');t.value=v;document.body.appendChild(t);t.select();document.execCommand('copy');t.remove()}};
const T={
 nodeExpress:(app,c)=>({backendAuth:`// ${app.display_name} — production VexaAccount SSO service\n// File: backend/src/auth/vexaaccount-sso.js\nconst crypto=require('crypto');\nconst config=JSON.parse(process.env.VEXA_ACCOUNT_SSO_CONFIG);\nconst CLIENT_ID=process.env.VEXA_ACCOUNT_CLIENT_ID||config.clientId;\nconst CLIENT_SECRET=process.env.VEXA_ACCOUNT_CLIENT_SECRET;\nif(!CLIENT_ID||!CLIENT_SECRET)throw new Error('VexaAccount SSO credentials are missing');\nconst states=new Map();\nfunction createState(){const state=crypto.randomBytes(32).toString('hex');states.set(state,Date.now()+300000);return state}\nfunction consumeState(state){const exp=states.get(state);states.delete(state);return Boolean(exp&&exp>Date.now())}\nfunction authorizationUrl(){const state=createState();const u=new URL(config.url+'/api/sso/authorize');u.searchParams.set('response_type','code');u.searchParams.set('client_id',CLIENT_ID);u.searchParams.set('redirect_uri',config.redirectUri);u.searchParams.set('scope',config.scopes.join(' '));u.searchParams.set('state',state);return {url:u.toString(),state}}\nasync function exchangeCode(code){const body=new URLSearchParams({grant_type:'authorization_code',code,client_id:CLIENT_ID,client_secret:CLIENT_SECRET,redirect_uri:config.redirectUri});const r=await fetch(config.url+'/api/sso/token',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body});const d=await r.json().catch(()=>({}));if(!r.ok||d.error)throw new Error(d.error_description||d.error||'VexaAccount token exchange failed');return d}\nasync function userInfo(token){const r=await fetch(config.url+'/api/sso/userinfo',{headers:{Authorization:'Bearer '+token}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'VexaAccount userinfo failed');return d}\nmodule.exports={createState,consumeState,authorizationUrl,exchangeCode,userInfo,config};`,backendRoute:`// ${app.display_name} — SSO routes\n// File: backend/src/routes/auth-vexaaccount.js\nconst express=require('express');\nconst {authorizationUrl,consumeState,exchangeCode,userInfo}=require('../auth/vexaaccount-sso');\nconst router=express.Router();\nrouter.get('/vexaaccount',(_req,res)=>res.redirect(authorizationUrl().url));\nrouter.get('/vexaaccount/callback',async(req,res,next)=>{try{const {code,state,error}=req.query;if(error)return res.redirect('/login?error='+encodeURIComponent(error));if(!code||!state||!consumeState(state))return res.status(400).json({error:'Invalid or expired SSO state'});const tokens=await exchangeCode(code);const account=await userInfo(tokens.access_token);\n// REQUIRED APP-SPECIFIC STEP: atomically upsert the local user by account.sub/email.\nconst user=await req.app.locals.userRepository.upsertFromVexaAccount(account);\nreq.session.userId=user.id;req.session.vexaAccount={sub:account.sub,email:account.email,name:account.name};\nres.redirect('/dashboard')}catch(e){next(e)}});\nrouter.post('/vexaaccount/logout',(req,res)=>{req.session.destroy(()=>res.json({success:true}))});\nmodule.exports=router;`,session:`// File: backend/src/middleware/require-vexaaccount-user.js\nmodule.exports=(req,res,next)=>{if(!req.session?.userId)return res.status(401).json({error:'AUTHENTICATION_REQUIRED'});next()};`,mapping:`-- File: database/vexaaccount-sso.sql\n-- Adapt column types/names to the target application's existing schema.\nCREATE TABLE IF NOT EXISTS vexaaccount_identities (id BIGINT PRIMARY KEY AUTO_INCREMENT,user_id BIGINT NOT NULL,provider VARCHAR(32) NOT NULL DEFAULT 'vexaaccount',subject_id VARCHAR(255) NOT NULL,email VARCHAR(320) NULL,profile_json JSON NULL,created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,UNIQUE KEY uq_vexa_subject(provider,subject_id),UNIQUE KEY uq_vexa_user(provider,user_id));`,frontend:`// File: frontend-user/src/services/vexaAccountSso.js\nexport function signInWithVexaAccount(){window.location.assign('/api/auth/vexaaccount')}\nexport async function getSession(){const r=await fetch('/api/auth/session',{credentials:'include'});if(!r.ok)return null;return r.json()}\nexport async function signOut(){await fetch('/api/auth/vexaaccount/logout',{method:'POST',credentials:'include'});window.location.assign('/login')}\nexport function mapProfile(a){return{id:a?.sub||a?.id||null,name:a?.name||a?.display_name||'',email:a?.email||'',firstName:a?.given_name||'',lastName:a?.family_name||''}}`,login:`// File: frontend-user/src/auth/VexaAccountLogin.jsx\nexport default function VexaAccountLogin(){return <button type="button" onClick={()=>window.location.assign('/api/auth/vexaaccount')}>Continue with VexaAccount</button>}`,guard:`// File: frontend-user/src/auth/VexaAccountAuthGuard.jsx\nimport {useEffect,useState} from 'react';import {getSession} from '../services/vexaAccountSso';\nexport default function VexaAccountAuthGuard({children}){const[s,setS]=useState(undefined);useEffect(()=>{getSession().then(setS)},[]);if(s===undefined)return <div>Checking session…</div>;if(!s)return <div>Authentication required.</div>;return children}`}),
 nextjs:(app,c)=>{const n=T.nodeExpress(app,c);n.frontend=n.frontend.replace("window.location.assign('/api/auth/vexaaccount')","window.location.assign('/api/auth/vexaaccount')");return {...n,backendRoute:n.backendRoute.replace("const express=require('express');","// Mount these handlers in your Next.js route handlers/API layer; keep the same server-only token exchange contract.\nconst express=require('express');")}},
 fastapi:(app,c)=>({backendAuth:`# ${app.display_name} — File: backend/app/auth/vexaaccount_sso.py\nimport os,secrets,time,httpx\nCONFIG=__import__('json').loads(os.environ['VEXA_ACCOUNT_SSO_CONFIG']);CLIENT_ID=os.getenv('VEXA_ACCOUNT_CLIENT_ID') or CONFIG['clientId'];CLIENT_SECRET=os.getenv('VEXA_ACCOUNT_CLIENT_SECRET')\n_STATES={}\ndef authorization_url():\n state=secrets.token_urlsafe(32);_STATES[state]=time.time()+300\n from urllib.parse import urlencode\n return CONFIG['url']+'/api/sso/authorize?'+urlencode({'response_type':'code','client_id':CLIENT_ID,'redirect_uri':CONFIG['redirectUri'],'scope':' '.join(CONFIG['scopes']),'state':state}),state\nasync def exchange_code(code):\n async with httpx.AsyncClient(timeout=CONFIG.get('timeoutMs',10000)/1000) as c:\n  r=await c.post(CONFIG['url']+'/api/sso/token',data={'grant_type':'authorization_code','code':code,'client_id':CLIENT_ID,'client_secret':CLIENT_SECRET,'redirect_uri':CONFIG['redirectUri']});r.raise_for_status();return r.json()\nasync def userinfo(token):\n async with httpx.AsyncClient(timeout=CONFIG.get('timeoutMs',10000)/1000) as c:\n  r=await c.get(CONFIG['url']+'/api/sso/userinfo',headers={'Authorization':'Bearer '+token});r.raise_for_status();return r.json()`,backendRoute:`# File: backend/app/routes/vexaaccount.py\n# FastAPI route skeleton: wire your existing session/user repository.\nfrom fastapi import APIRouter,Request,HTTPException\nfrom fastapi.responses import RedirectResponse\nfrom .auth.vexaaccount_sso import authorization_url,exchange_code,userinfo\nrouter=APIRouter(prefix='/api/auth')\n@router.get('/vexaaccount')\nasync def login(): return RedirectResponse(authorization_url()[0])\n@router.get('/vexaaccount/callback')\nasync def callback(code:str,state:str,request:Request):\n # Validate state against your server-side store, then exchange code and upsert local user.\n tokens=await exchange_code(code);account=await userinfo(tokens['access_token']);request.session['user_id']=await request.app.state.user_repository.upsert_from_vexaaccount(account);return RedirectResponse('/dashboard')`,session:`# File: backend/app/middleware/vexaaccount_session.py\n# Require your framework's server-side session middleware and reject missing local user_id.`,mapping:`-- File: database/vexaaccount-sso.sql\nCREATE TABLE IF NOT EXISTS vexaaccount_identities (id BIGINT PRIMARY KEY AUTO_INCREMENT,user_id BIGINT NOT NULL,provider VARCHAR(32) NOT NULL DEFAULT 'vexaaccount',subject_id VARCHAR(255) NOT NULL,email VARCHAR(320),profile_json JSON,created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,UNIQUE KEY uq_vexa_subject(provider,subject_id));`,frontend:`// File: frontend-user/src/services/vexaAccountSso.js\nexport const signInWithVexaAccount=()=>window.location.assign('/api/auth/vexaaccount');\nexport const getSession=async()=>{const r=await fetch('/api/auth/session',{credentials:'include'});return r.ok?r.json():null};`,login:`// File: frontend-user/src/auth/VexaAccountLogin.jsx\nexport default function VexaAccountLogin(){return <button onClick={()=>window.location.assign('/api/auth/vexaaccount')}>Continue with VexaAccount</button>}`,guard:`// File: frontend-user/src/auth/VexaAccountAuthGuard.jsx\nexport default function VexaAccountAuthGuard({children}){return children}`}),
 django:(app,c)=>T.fastapi(app,c)
};
function cfgFrom(d,id){const raw=d?.environment?.VEXA_ACCOUNT_SSO_CONFIG||d?.VEXA_ACCOUNT_SSO_CONFIG||'';const p=typeof raw==='string'?(()=>{try{return JSON.parse(raw)}catch{return {}}})():(raw||{});return{url:String(p.url||API).replace(/\/$/,''),clientId:p.clientId||id,redirectUri:p.redirectUri||'',scopes:Array.isArray(p.scopes)?p.scopes:[],timeoutMs:Number(p.timeoutMs)||10000}}
function templateFor(framework,app,cfg){const fn=T[framework]||T.nodeExpress;const x=fn(app,cfg);const files=[['backend/src/auth/vexaaccount-sso.js',x.backendAuth],['backend/src/routes/auth-vexaaccount.js',x.backendRoute],['backend/src/middleware/require-vexaaccount-user.js',x.session],['database/vexaaccount-sso.sql',x.mapping],['frontend-user/src/services/vexaAccountSso.js',x.frontend],['frontend-user/src/auth/VexaAccountLogin.jsx',x.login],['frontend-user/src/auth/VexaAccountAuthGuard.jsx',x.guard]];return files.filter(x=>x[1])}
function commonFiles(app,cfg,framework,frontend){const json=JSON.stringify({application:app.display_name,clientId:cfg.clientId,authorizationEndpoint:cfg.url+'/api/sso/authorize',tokenEndpoint:cfg.url+'/api/sso/token',userinfoEndpoint:cfg.url+'/api/sso/userinfo',redirectUri:cfg.redirectUri,scopes:cfg.scopes,timeoutMs:cfg.timeoutMs,framework,frontend},null,2);const env=`VEXA_ACCOUNT_CLIENT_ID=${cfg.clientId}\nVEXA_ACCOUNT_CLIENT_SECRET=PASTE_ONE_TIME_SECRET_HERE\nVEXA_ACCOUNT_SSO_CONFIG=${JSON.stringify(cfg)}`;const test=`// File: tests/vexaaccount-sso.integration.test.js\n// Generated verification contract. Run only against a test/staging app.\n// 1. GET /api/auth/vexaaccount returns a VexaAccount authorization redirect.\n// 2. Callback rejects missing/expired state.\n// 3. Callback rejects token exchange errors.\n// 4. Successful callback creates/updates exactly one local identity keyed by VexaAccount subject.\n// 5. Browser bundle contains no VEXA_ACCOUNT_CLIENT_SECRET.\n// 6. Logout destroys the local session.\n// 7. Protected routes reject unauthenticated requests.\n`;const deploy=`# File: deployment/vexaaccount-sso.md\n## Production checklist\n- Set VEXA_ACCOUNT_CLIENT_ID, VEXA_ACCOUNT_CLIENT_SECRET and VEXA_ACCOUNT_SSO_CONFIG in the backend secret store.\n- Register the exact HTTPS callback URI in VexaAccount before deployment.\n- Never put the client secret in frontend code, localStorage, URLs or source control.\n- Run database migration before enabling the login button.\n- Verify callback, user mapping, session creation, logout and protected-route behavior in staging.\n- Confirm the registered application status is active before production traffic.\n`;const readme=`# ${app.display_name} — VexaAccount SSO Integration\nGenerated from the authoritative VexaAccount SSO registry.\n\nFramework: ${framework}; Frontend: ${frontend}\nRedirect URI: ${cfg.redirectUri}\nScopes: ${cfg.scopes.join(', ')||'none'}\n\nThe generated package separates browser-safe configuration from the backend-only client secret. The user mapping section is intentionally an adapter point for the target application's existing user schema; it must not overwrite unrelated account data.\n`;return [['backend/.env.example',env],['integration-config.json',json],['tests/vexaaccount-sso.integration.test.js',test],['deployment/vexaaccount-sso.md',deploy],['VEXAACCOUNT-SSO-SETUP.md',readme]]}
function patchManifest(app,cfg,framework,frontend){return JSON.stringify({format:'vexaaccount-sso-patch-v1',application:app.display_name,framework,frontend,operations:[{type:'add',path:'backend/src/auth/vexaaccount-sso.js',purpose:'server-side authorization URL, state, token exchange and userinfo'},{type:'add',path:'backend/src/routes/auth-vexaaccount.js',purpose:'login/callback/logout routes'},{type:'add',path:'backend/src/middleware/require-vexaaccount-user.js',purpose:'local session guard'},{type:'add',path:'database/vexaaccount-sso.sql',purpose:'VexaAccount subject to local user mapping'},{type:'add',path:'frontend-user/src/services/vexaAccountSso.js',purpose:'browser-safe SSO client'},{type:'add',path:'frontend-user/src/auth/VexaAccountLogin.jsx',purpose:'login UI'},{type:'add',path:'frontend-user/src/auth/VexaAccountAuthGuard.jsx',purpose:'protected UI boundary'},{type:'add',path:'backend/.env.example',purpose:'environment contract'},{type:'add',path:'tests/vexaaccount-sso.integration.test.js',purpose:'E2E verification contract'},{type:'add',path:'deployment/vexaaccount-sso.md',purpose:'deployment validation'}],authoritativeConfig:cfg,security:{secret:'backend-only; paste one-time secret into secret manager',automaticWrite:'not performed by browser'}} ,null,2)}
async function openGenerator(id){try{const[a,c]=await Promise.all([api('/api/sso-registry/applications/'+encodeURIComponent(id)),api('/api/sso-registry/applications/'+encodeURIComponent(id)+'/integration-config')]);const app=a.application||{};const cfg=cfgFrom(c,id);const m=document.createElement('div');m.className='os-modal';m.innerHTML=`<section class="os-modal-card os-generator-modal"><div class="os-modal-head"><div><p class="os-eyebrow">PRODUCTION SSO INTEGRATION FACTORY</p><h2>${esc(app.display_name||id)}</h2><p class="os-muted">Authoritative registry → app-specific backend, frontend-user, database mapping, tests, deployment and patch manifest.</p></div><button type="button" class="os-btn" data-gen-close>Close</button></div><div class="os-form-grid"><label class="os-field">Backend framework<select id="gen-framework"><option value="nodeExpress">Node / Express</option><option value="nextjs">Next.js server integration</option><option value="fastapi">Python / FastAPI</option><option value="django">Python / Django adapter</option></select></label><label class="os-field">Frontend<select id="gen-frontend"><option>React</option><option>Next.js</option><option>Vue</option><option>Vanilla JS</option></select></label></div><div class="os-panel"><h3>Authoritative integration</h3><div class="os-list"><div class="os-row"><span>Client ID</span><b>${esc(cfg.clientId)}</b></div><div class="os-row"><span>Redirect URI</span><b>${esc(cfg.redirectUri||'Not registered')}</b></div><div class="os-row"><span>Scopes</span><b>${esc(cfg.scopes.join(', ')||'none')}</b></div></div></div><div class="os-actions"><button type="button" class="os-btn os-primary" data-gen-build>Generate complete package</button></div><div id="gen-output"></div></section>`;document.body.appendChild(m);m.querySelector('[data-gen-close]').onclick=()=>m.remove();m.querySelector('[data-gen-build]').onclick=()=>{const framework=$('#gen-framework',m).value;const frontend=$('#gen-frontend',m).value;const files=templateFor(framework,app,cfg);files.push(...commonFiles(app,cfg,framework,frontend));files.push(['integration-patch-manifest.json',patchManifest(app,cfg,framework,frontend)]);const out=$('#gen-output',m);out.innerHTML=`<div class="os-panel"><h3>Generated source package · ${files.length} files</h3><p class="os-muted">Automatic patching is represented by a deterministic manifest. The Owner panel does not write into another application's repository or expose its client secret.</p><div class="os-list">${files.map((f,i)=>`<article class="os-card"><div class="os-card-main"><b>${esc(f[0])}</b><small>${i<7?'Application source':'Integration tooling'}</small><pre class="os-code os-code-block">${esc(f[1])}</pre></div><div class="os-actions"><button class="os-btn os-primary" data-copy="${i}">Copy file</button></div></article>`).join('')}</div><div class="os-actions"><button class="os-btn" data-copy-all>Copy all as manifest</button><button class="os-btn" data-validate>Run local package validation</button></div><p class="os-muted" data-validation></p></div>`;out.querySelectorAll('[data-copy]').forEach(b=>b.onclick=async()=>{await copy(files[Number(b.dataset.copy)].join('\n'));b.textContent='Copied ✓';setTimeout(()=>b.textContent='Copy file',1200)});out.querySelector('[data-copy-all]').onclick=async()=>{await copy(JSON.stringify(Object.fromEntries(files),null,2));toast('Package manifest copied')};out.querySelector('[data-validate]').onclick=()=>{const problems=[];if(!cfg.redirectUri)problems.push('No registered redirect URI');if(!cfg.scopes.length)problems.push('No scopes configured');if(!cfg.clientId)problems.push('Missing client ID');out.querySelector('[data-validation]').textContent=problems.length?'Validation needs attention: '+problems.join(' · '):'Registry package validation passed: client ID, redirect URI and scopes are present. Run the generated staging E2E tests before production.'};window.__VEXA_LAST_GENERATED_PACKAGE__={app,cfg,framework,frontend,files}};window.vexaSsoGenerator={open:openGenerator}}catch(e){alert(e.message)}}
function inject(){document.querySelectorAll('.os-page-head').forEach(h=>{if(h.dataset.genInjected)return;const t=h.querySelector('h2');if(!t||!t.textContent.includes('Application Detail'))return;const p=h.querySelector('.os-actions')||h;const id=(location.hash.match(/app:([^#]+)/)||[])[1];if(!id)return;const b=document.createElement('button');b.type='button';b.className='os-btn os-primary';b.textContent='Generate Complete SSO Package';b.onclick=()=>openGenerator(decodeURIComponent(id));p.appendChild(b);h.dataset.genInjected='1'});document.querySelectorAll('.os-card-actions').forEach(x=>{if(x.querySelector('[data-gen-inline]'))return;const open=x.querySelector('[data-os-action^="sso-open:"]');if(!open)return;const id=open.dataset.osAction.slice(9);const b=document.createElement('button');b.type='button';b.className='os-btn';b.textContent='Generate';b.dataset.genInline='1';b.onclick=()=>openGenerator(id);x.appendChild(b)})}
new MutationObserver(inject).observe(document.documentElement,{subtree:true,childList:true});setInterval(inject,1000);window.vexaSsoGenerator={open:openGenerator};
})();