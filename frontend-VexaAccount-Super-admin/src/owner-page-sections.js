(()=>{'use strict';
/* Legacy section renderer retired. The current Owner OS is implemented by owner-separate-pages.js.
   Kept only as a compatibility stub so stale cached references cannot recreate the old Control System UI. */
window.__VEXA_OWNER_PAGE_SECTIONS_RETIRED__=true;
})();
