const CACHE='vexaaccount-owner-v2';
self.addEventListener('install',e=>e.waitUntil(caches.open(CACHE).then(c=>c.addAll(['/','/manifest.webmanifest'])).catch(()=>{}).then(()=>self.skipWaiting())));
self.addEventListener('activate',e=>e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k.startsWith('vexaaccount-owner-')&&k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim())));
self.addEventListener('fetch',e=>{if(e.request.method!=='GET')return;const u=new URL(e.request.url);if(u.origin!==self.location.origin||u.pathname.startsWith('/api/'))return;e.respondWith(fetch(e.request).then(r=>{if(r.ok)caches.open(CACHE).then(c=>c.put(e.request,r.clone()));return r}).catch(()=>caches.match(e.request).then(r=>r||caches.match('/'))))});
self.addEventListener('push',event=>{let d={};try{d=event.data?.json()||{}}catch{d={title:'VexaAccount Owner',body:event.data?.text()||'You have a new owner notification.'}}event.waitUntil(self.registration.showNotification(d.title||'VexaAccount Owner',{body:d.body||'',icon:'/src/owner-icon.svg',badge:'/src/owner-icon.svg',tag:d.tag||'vexa-owner',renotify:true,data:d.data||{url:'/'}}))});
self.addEventListener('notificationclick',event=>{event.notification.close();const target=event.notification.data?.url||'/';event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const c of list){if('focus'in c){try{c.navigate(new URL(target,self.location.origin).href)}catch{}return c.focus()}}return clients.openWindow(new URL(target,self.location.origin).href)}))});