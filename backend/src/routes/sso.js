const express=require('express');const crypto=require('crypto');const jwt=require('jsonwebtoken');const{pool}=require('../config/database');const{requireSsoUser}=require('../middleware/ssoUserAuth');const router=express.Router();const JWT_SECRET=process.env.JWT_SECRET;const ISSUER=process.env.VEXA_ACCOUNT_ISSUER||'https://api-vexaaccount.onrender.com';const CODE_TTL_SECONDS=300;const ACCESS_TTL_SECONDS=3600;const REFRESH_TTL_DAYS=30;const SUPPORTED_SCOPES=['openid','profile','email','account','session','applications','notifications'];if(!JWT_SECRET)throw new Error('JWT_SECRET must be configured for VexaAccount SSO');function sha256(value){return crypto.createHash('sha256').update(String(value)).digest('base64url')}function legacySha256(value){return crypto.createHash('sha256').update(String(value)).digest('hex')}function randomToken(bytes=32){return crypto.randomBytes(bytes).toString('base64url')}function safeEqual(a,b){const aa=Buffer.from(String(a)),bb=Buffer.from(String(b));return aa.length===bb.length&&crypto.timingSafeEqual(aa,bb)}function parseJsonArray(value,fallback=[]){if(Array.isArray(value))return value;try{const parsed=JSON.parse(value??'[]');return Array.isArray(parsed)?parsed:fallback}catch{return fallback}}async function getUserSessionVersion(id,connection=pool){const[rows]=await connection.query('SELECT session_version FROM store_users WHERE id=? AND is_active=1 LIMIT 1',[id]);return rows[0]?Number(rows[0].session_version||1):null}async function getClient(id,connection=pool){const[rows]=await connection.query('SELECT id,client_id,client_secret_hash,name,redirect_uris,allowed_scopes,is_active FROM sso_clients WHERE client_id=? LIMIT 1',[id]);return rows[0]||null}function redirectAllowed(client,uri){return Boolean(client&&Number(client.is_active)===1&&uri&&parseJsonArray(client.redirect_uris).some(value=>String(value).trim()===String(uri).trim()))}function clientSecretValid(client,secret){if(!client||!client.client_secret_hash||!secret)return false;const stored=String(client.client_secret_hash);return safeEqual(sha256(secret),stored)||safeEqual(legacySha256(secret),stored)}function requestedScopes(value){return[...new Set(String(value||'').split(/\s+/).filter(Boolean))]}async function getActiveConsent(connection,clientId,userId,requestedScope){const[rows]=await connection.query('SELECT scopes,revoked_at FROM sso_consents WHERE client_id=? AND user_id=? LIMIT 1 FOR UPDATE',[clientId,userId]);if(!rows.length||rows[0].revoked_at)return null;const granted=new Set(requestedScopes(rows[0].scopes));const scope=requestedScopes(requestedScope);if(scope.some(item=>!granted.has(item)))return null;return rows[0]}
router.get('/.well-known/openid-configuration',(req,res)=>res.json({issuer:ISSUER,authorization_endpoint:`${ISSUER}/api/sso/authorize`,token_endpoint:`${ISSUER}/api/sso/token`,userinfo_endpoint:`${ISSUER}/api/sso/userinfo`,end_session_endpoint:`${ISSUER}/api/sso/logout`,response_types_supported:['code'],grant_types_supported:['authorization_code','refresh_token'],code_challenge_methods_supported:['S256'],scopes_supported:SUPPORTED_SCOPES,token_endpoint_auth_methods_supported:['client_secret_post']}));
router.get('/authorize',requireSsoUser,async(req,res,next)=>{try{const{client_id:clientId,redirect_uri:redirectUri,response_type:responseType,scope='openid profile email',state,code_challenge:codeChallenge,code_challenge_method:codeChallengeMethod}=req.query;if(responseType!=='code'||!clientId||!redirectUri||!state||!codeChallenge||codeChallengeMethod!=='S256')return res.status(400).json({success:false,message:'Valid authorization code + S256 PKCE parameters are required'});const client=await getClient(clientId);if(!redirectAllowed(client,redirectUri))return res.status(400).json({success:false,message:'Invalid SSO client or redirect URI'});const allowed=new Set(parseJsonArray(client.allowed_scopes,['openid','profile','email']));const scopes=requestedScopes(scope);if(!scopes.length||scopes.some(item=>!SUPPORTED_SCOPES.includes(item)||!allowed.has(item)))return res.status(400).json({success:false,message:'Requested scope is not allowed'});const code=randomToken(32);await pool.query('INSERT INTO sso_authorization_codes (code_hash,client_id,user_id,redirect_uri,scope,code_challenge,expires_at) VALUES (?,?,?,?,?,?,DATE_ADD(NOW(),INTERVAL ? SECOND))',[sha256(code),clientId,req.user.id,redirectUri,scopes.join(' '),codeChallenge,CODE_TTL_SECONDS]);await pool.query(`INSERT INTO sso_consents (client_id,user_id,scopes,granted_at,revoked_at) VALUES (?,?,?,NOW(),NULL) ON DUPLICATE KEY UPDATE scopes=VALUES(scopes),granted_at=NOW(),revoked_at=NULL`,[clientId,req.user.id,scopes.join(' ')]);await pool.query('INSERT INTO sso_security_events (user_id,client_id,event_type,ip_address,user_agent,metadata) VALUES (?,?,?,?,?,?)',[req.user.id,clientId,'sso.authorization_issued',req.ip||null,String(req.get('user-agent')||'').slice(0,512)||null,JSON.stringify({scopes})]);const separator=redirectUri.includes('?')?'&':'?';const redirectUrl=`${redirectUri}${separator}code=${encodeURIComponent(code)}&state=${encodeURIComponent(state)}`;if(req.get('x-vexa-sso-frontend')==='1'||String(req.get('accept')||'').includes('application/vnd.vexa.sso+json'))return res.json({success:true,redirectUrl,code_issued:true});return res.redirect(redirectUrl)}catch(error){return next(error)}});
router.post('/token',async(req,res,next)=>{const connection=await pool.getConnection();let transactionStarted=false;try{const{grant_type:grantType,code,redirect_uri:redirectUri,client_id:clientId,client_secret:clientSecret,code_verifier:codeVerifier,refresh_token:refreshToken}=req.body||{};if(!clientId)return res.status(400).json({success:false,message:'client_id is required'});const client=await getClient(clientId,connection);if(!client||Number(client.is_active)!==1||!clientSecretValid(client,clientSecret))return res.status(401).json({success:false,message:'Client authentication failed'});if(grantType==='refresh_token'){if(!refreshToken)return res.status(400).json({success:false,message:'refresh_token is required'});await connection.beginTransaction();transactionStarted=true;const[rows]=await connection.query('SELECT * FROM sso_refresh_tokens WHERE token_hash=? AND client_id=? AND revoked_at IS NULL AND expires_at>NOW() LIMIT 1 FOR UPDATE',[sha256(refreshToken),clientId]);if(!rows.length){await connection.rollback();transactionStarted=false;return res.status(401).json({success:false,message:'Invalid or expired refresh token'})}const session=rows[0];const sv=await getUserSessionVersion(session.user_id,connection);if(sv===null){await connection.rollback();transactionStarted=false;return res.status(401).json({success:false,message:'User account is inactive or unavailable'})}const consent=await getActiveConsent(connection,clientId,session.user_id,session.scope);if(!consent){await connection.rollback();transactionStarted=false;return res.status(401).json({success:false,message:'SSO consent is revoked or no longer grants this scope'})}const replacement=randomToken(48);await connection.query('UPDATE sso_refresh_tokens SET revoked_at=NOW() WHERE id=? AND revoked_at IS NULL',[session.id]);await connection.query('INSERT INTO sso_refresh_tokens (token_hash,client_id,user_id,scope,expires_at) VALUES (?,?,?,?,DATE_ADD(NOW(),INTERVAL ? DAY))',[sha256(replacement),clientId,session.user_id,session.scope,REFRESH_TTL_DAYS]);await connection.query('UPDATE sso_clients SET last_used_at=NOW() WHERE client_id=?',[clientId]);await connection.query('UPDATE sso_sessions SET last_seen_at=NOW() WHERE client_id=? AND user_id=? AND revoked_at IS NULL',[clientId,session.user_id]);await connection.commit();transactionStarted=false;const accessToken=jwt.sign({sub:String(session.user_id),client_id:clientId,scope:session.scope},JWT_SECRET,{issuer:ISSUER,expiresIn:ACCESS_TTL_SECONDS});return res.json({access_token:accessToken,token_type:'Bearer',expires_in:ACCESS_TTL_SECONDS,refresh_token:replacement,scope:session.scope})}
if(grantType!=='authorization_code')return res.status(400).json({success:false,message:'Unsupported grant_type'});if(!code||!redirectUri||!codeVerifier)return res.status(400).json({success:false,message:'code, redirect_uri and code_verifier are required'});await connection.beginTransaction();transactionStarted=true;const[rows]=await connection.query('SELECT * FROM sso_authorization_codes WHERE code_hash=? AND client_id=? AND revoked_at IS NULL AND expires_at>NOW() LIMIT 1 FOR UPDATE',[sha256(code),clientId]);if(!rows.length){await connection.rollback();transactionStarted=false;return res.status(401).json({success:false,message:'Invalid or expired authorization code'})}const authCode=rows[0];if(String(authCode.redirect_uri)!==String(redirectUri)||sha256(codeVerifier)!==String(authCode.code_challenge)){await connection.rollback();transactionStarted=false;return res.status(401).json({success:false,message:'Authorization code validation failed'})}await connection.query('UPDATE sso_authorization_codes SET revoked_at=NOW() WHERE id=?',[authCode.id]);const sv=await getUserSessionVersion(authCode.user_id,connection);if(sv===null){await connection.rollback();transactionStarted=false;return res.status(401).json({success:false,message:'User account is inactive or unavailable'})}const accessToken=jwt.sign({sub:String(authCode.user_id),client_id:clientId,scope:authCode.scope,sv},JWT_SECRET,{issuer:ISSUER,expiresIn:ACCESS_TTL_SECONDS});const replacement=randomToken(48);await connection.query('INSERT INTO sso_refresh_tokens (token_hash,client_id,user_id,scope,expires_at) VALUES (?,?,?,?,DATE_ADD(NOW(),INTERVAL ? DAY))',[sha256(replacement),clientId,authCode.user_id,authCode.scope,REFRESH_TTL_DAYS]);await connection.query('INSERT INTO sso_sessions (client_id,user_id,scope,access_expires_at,expires_at,last_seen_at) VALUES (?,?,?,DATE_ADD(NOW(),INTERVAL ? SECOND),DATE_ADD(NOW(),INTERVAL ? DAY),NOW())',[clientId,authCode.user_id,authCode.scope,ACCESS_TTL_SECONDS,REFRESH_TTL_DAYS]);await connection.query('UPDATE sso_clients SET last_used_at=NOW() WHERE client_id=?',[clientId]);await connection.commit();transactionStarted=false;return res.json({access_token:accessToken,token_type:'Bearer',expires_in:ACCESS_TTL_SECONDS,refresh_token:replacement,scope:authCode.scope})}catch(error){if(transactionStarted)await connection.rollback().catch(()=>{});return next(error)}finally{connection.release()}});
router.get('/userinfo',requireSsoUser,async(req,res)=>{const scope=new Set(requestedScopes(req.user?.scope||''));const profile={sub:String(req.user.id)};if(scope.has('profile')){profile.name=req.user.name||null;profile.avatar_url=req.user.avatar_url||null}if(scope.has('email'))profile.email=req.user.email;if(scope.has('account'))profile.account={id:req.user.id,is_verified:Boolean(req.user.is_verified)}if(scope.has('session'))profile.session={session_version:Number(req.user.session_version||1)}res.json(profile)});
router.get('/logout',requireSsoUser,async(req,res)=>{try{const clientId=String(req.query.client_id||req.user?.client_id||'').trim();if(clientId){await pool.query('UPDATE sso_sessions SET revoked_at=NOW() WHERE client_id=? AND user_id=? AND revoked_at IS NULL',[clientId,req.user.id]);await pool.query('UPDATE sso_refresh_tokens SET revoked_at=NOW() WHERE client_id=? AND user_id=? AND revoked_at IS NULL',[clientId,req.user.id])}res.json({success:true,message:'SSO session revoked'})}catch(e){res.status(500).json({success:false,message:'Unable to revoke SSO session'})}});
module.exports=router;
