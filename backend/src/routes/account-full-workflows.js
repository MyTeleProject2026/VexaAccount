const express=require('express');
const jwt=require('jsonwebtoken');
const bcrypt=require('bcryptjs');
const {pool}=require('../config/database');
const {sendOtpEmail,sendResetEmail}=require('../services/emailService');
const router=express.Router();
const JWT_SECRET=process.env.JWT_SECRET;
const IS_PRODUCTION=process.env.NODE_ENV==='production';
const otp=()=>String(Math.floor(100000+Math.random()*900000));
function getToken(req){const h=req.get('authorization')||'';return h.startsWith('Bearer ')?h.slice(7).trim():req.cookies?.vexaccount_session||null}
function requireUser(req,res,next){try{const t=getToken(req);if(!t)return res.status(401).json({success:false,message:'Authentication required'});const c=jwt.verify(t,JWT_SECRET),id=c.sub||c.id;if(!id||c.role!=='user')return res.status(401).json({success:false,message:'User authentication required'});req.userId=id;next()}catch(e){return res.status(401).json({success:false,message:'Invalid or expired session'})}}
router.use(requireUser);
async function ensureSettings(id){await pool.query('INSERT IGNORE INTO vexa_account_center_settings (user_id) VALUES (?)',[id])}
router.get('/privacy',async(req,res,next)=>{try{await ensureSettings(req.userId);const [s]=await pool.query('SELECT location_sharing_enabled,personalization_enabled,activity_history_enabled,push_notifications_enabled,product_updates_enabled,updated_at FROM vexa_account_center_settings WHERE user_id=?',[req.userId]);const [p]=await pool.query('SELECT locale,timezone,marketing_email_enabled,security_email_enabled FROM vexa_account_preferences WHERE user_id=?',[req.userId]);res.json({success:true,privacy:{...(s[0]||{}),...(p[0]||{})}})}catch(e){next(e)}});
router.patch('/privacy',async(req,res,next)=>{try{await ensureSettings(req.userId);const p=req.body||{};const settingKeys=['location_sharing_enabled','personalization_enabled','activity_history_enabled','push_notifications_enabled','product_updates_enabled'];const prefKeys=['marketing_email_enabled','security_email_enabled'];const sf=[],sv=[],pf=[],pv=[];for(const k of settingKeys)if(typeof p[k]==='boolean'){sf.push(`${k}=?`);sv.push(p[k]?1:0)}for(const k of prefKeys)if(typeof p[k]==='boolean'){pf.push(`${k}=?`);pv.push(p[k]?1:0)}if(sf.length){sv.push(req.userId);await pool.query(`UPDATE vexa_account_center_settings SET ${sf.join(',')} WHERE user_id=?`,sv)}if(pf.length){await pool.query('INSERT IGNORE INTO vexa_account_preferences(user_id) VALUES(?)',[req.userId]);pv.push(req.userId);await pool.query(`UPDATE vexa_account_preferences SET ${pf.join(',')} WHERE user_id=?`,pv)}if(!sf.length&&!pf.length)return res.status(400).json({success:false,message:'No privacy changes supplied'});res.json({success:true,message:'Privacy settings updated'})}catch(e){next(e)}});
router.get('/devices',async(req,res,next)=>{try{const [rows]=await pool.query(`SELECT s.id,s.client_id,COALESCE(r.display_name,s.client_id) display_name,s.scope,s.created_at,s.last_seen_at,s.expires_at,s.user_agent,s.ip_address FROM sso_sessions s LEFT JOIN sso_client_registry r ON r.client_id=s.client_id WHERE s.user_id=? AND s.revoked_at IS NULL AND s.expires_at>NOW() ORDER BY s.last_seen_at DESC`,[req.userId]);res.json({success:true,devices:rows})}catch(e){next(e)}});
router.delete('/devices/:id',async(req,res,next)=>{try{const [r]=await pool.query('UPDATE sso_sessions SET revoked_at=NOW() WHERE id=? AND user_id=? AND revoked_at IS NULL',[req.params.id,req.userId]);if(!r.affectedRows)return res.status(404).json({success:false,message:'Device session not found'});await pool.query('INSERT INTO sso_security_events(user_id,event_type,metadata) VALUES(?,?,?)',[req.userId,'account.device_revoked',JSON.stringify({session_id:req.params.id})]);res.json({success:true,message:'Device access revoked'})}catch(e){next(e)}});
router.post('/devices/revoke-all',async(req,res,next)=>{try{const [r]=await pool.query('UPDATE sso_sessions SET revoked_at=NOW() WHERE user_id=? AND revoked_at IS NULL',[req.userId]);await pool.query('INSERT INTO sso_security_events(user_id,event_type,metadata) VALUES(?,?,?)',[req.userId,'account.devices_revoked_all',JSON.stringify({count:r.affectedRows})]);res.json({success:true,message:'All other device sessions revoked',count:r.affectedRows})}catch(e){next(e)}});
router.get('/people',async(req,res,next)=>{try{await ensureSettings(req.userId);const [r]=await pool.query('SELECT username,recovery_email,location_sharing_enabled,personalization_enabled FROM vexa_account_center_settings WHERE user_id=?',[req.userId]);res.json({success:true,sharing:r[0]||{}})}catch(e){next(e)}});
router.patch('/people',async(req,res,next)=>{try{await ensureSettings(req.userId);const p=req.body||{},allowed=['username','recovery_email','location_sharing_enabled','personalization_enabled'];const f=[],v=[];for(const k of allowed)if(Object.prototype.hasOwnProperty.call(p,k)){if(k==='username'){const x=String(p[k]||'').trim().toLowerCase();if(x&&!/^[a-z0-9._-]{3,64}$/.test(x))return res.status(400).json({success:false,message:'Username must be 3-64 characters using letters, numbers, dot, underscore or hyphen.'});f.push('username=?');v.push(x||null)}else if(k==='recovery_email'){const x=String(p[k]||'').trim().toLowerCase();if(x&&!/^[^@\\s]+@[^@\\s]+\\.[^@\\s]+$/.test(x))return res.status(400).json({success:false,message:'Enter a valid recovery email.'});f.push('recovery_email=?');v.push(x||null)}else if(typeof p[k]==='boolean'){f.push(`${k}=?`);v.push(p[k]?1:0)}}if(!f.length)return res.status(400).json({success:false,message:'No sharing changes supplied'});v.push(req.userId);await pool.query(`UPDATE vexa_account_center_settings SET ${f.join(',')} WHERE user_id=?`,v);res.json({success:true,message:'People & sharing settings updated'})}catch(e){if(e.code==='ER_DUP_ENTRY')return res.status(409).json({success:false,message:'Username is already in use'});next(e)}});
router.get('/verification',async(req,res,next)=>{try{const [u]=await pool.query('SELECT email,is_verified FROM store_users WHERE id=?',[req.userId]);res.json({success:true,verification:{email:u[0]?.email||'',is_verified:Boolean(u[0]?.is_verified)}})}catch(e){next(e)}});
router.post('/verification/resend',async(req,res,next)=>{try{const [u]=await pool.query('SELECT email,is_verified FROM store_users WHERE id=? AND is_active=1',[req.userId]);if(!u.length)return res.status(404).json({success:false,message:'User not found'});if(u[0].is_verified)return res.status(409).json({success:false,message:'Email is already verified'});const code=otp();await pool.query('UPDATE otp_codes SET is_used=1 WHERE user_id=? AND purpose="email_verification" AND is_used=0',[req.userId]);await pool.query('INSERT INTO otp_codes(user_id,otp_code,purpose,expires_at) VALUES(?,?,"email_verification",?)',[req.userId,code,new Date(Date.now()+600000)]);await sendOtpEmail(u[0].email,code);res.json({success:true,message:'Verification code sent to your email'})}catch(e){if(e.response){}next(e)}});
router.post('/verification/confirm',async(req,res,next)=>{try{const code=String(req.body.otp||'').trim();if(!/^\\d{6}$/.test(code))return res.status(400).json({success:false,message:'Enter the 6-digit verification code'});const [r]=await pool.query('SELECT id,expires_at FROM otp_codes WHERE user_id=? AND otp_code=? AND purpose="email_verification" AND is_used=0 ORDER BY id DESC LIMIT 1',[req.userId,code]);if(!r.length)return res.status(400).json({success:false,message:'Invalid verification code'});if(new Date()>new Date(r[0].expires_at))return res.status(400).json({success:false,message:'Verification code expired'});await pool.query('UPDATE otp_codes SET is_used=1 WHERE id=?',[r[0].id]);await pool.query('UPDATE store_users SET is_verified=1 WHERE id=?',[req.userId]);res.json({success:true,message:'Email verified successfully'})}catch(e){next(e)}});
router.get('/recovery',async(req,res,next)=>{try{await ensureSettings(req.userId);const [r]=await pool.query('SELECT recovery_email FROM vexa_account_center_settings WHERE user_id=?',[req.userId]);const [u]=await pool.query('SELECT email,is_verified FROM store_users WHERE id=?',[req.userId]);res.json({success:true,recovery:{recovery_email:r[0]?.recovery_email||'',primary_email:u[0]?.email||'',email_verified:Boolean(u[0]?.is_verified)}})}catch(e){next(e)}});
router.patch('/recovery',async(req,res,next)=>{try{await ensureSettings(req.userId);const email=String(req.body.recovery_email||'').trim().toLowerCase();if(email&&!/^[^@\\s]+@[^@\\s]+\\.[^@\\s]+$/.test(email))return res.status(400).json({success:false,message:'Enter a valid recovery email.'});await pool.query('UPDATE vexa_account_center_settings SET recovery_email=? WHERE user_id=?',[email||null,req.userId]);res.json({success:true,message:'Recovery email updated'})}catch(e){next(e)}});
router.post('/recovery/test',async(req,res,next)=>{try{await ensureSettings(req.userId);const [r]=await pool.query('SELECT recovery_email FROM vexa_account_center_settings WHERE user_id=?',[req.userId]);if(!r[0]?.recovery_email)return res.status(400).json({success:false,message:'Add a recovery email first'});const token=jwt.sign({id:req.userId,purpose:'password_reset'},JWT_SECRET,{expiresIn:'1h'});await pool.query('INSERT INTO otp_codes(user_id,otp_code,purpose,expires_at) VALUES(?,?,"password_reset",DATE_ADD(NOW(),INTERVAL 1 HOUR)) ON DUPLICATE KEY UPDATE otp_code=VALUES(otp_code),expires_at=VALUES(expires_at),is_used=0',[req.userId,token]);const base=String(process.env.FRONTEND_USER_URL||'https://vexaaccount-management.onrender.com').replace(/\\/$/,'');await sendResetEmail(r[0].recovery_email,`${base}/#/reset-password?token=${encodeURIComponent(token)}`);res.json({success:true,message:'Recovery test email sent'})}catch(e){next(e)}});
router.post('/export',async(req,res,next)=>{try{const [rows]=await pool.query('SELECT id,email,name,avatar_url,phone,bio,first_name,last_name,gender,dob,country,is_verified,is_active,created_at FROM store_users WHERE id=?',[req.userId]);if(!rows.length)return res.status(404).json({success:false,message:'User not found'});res.json({success:true,data:{exported_at:new Date().toISOString(),profile:rows[0]},message:'Your account export is ready'})}catch(e){next(e)}});
router.delete('/activity',async(req,res,next)=>{try{await pool.query('DELETE FROM sso_security_events WHERE user_id=?',[req.userId]);res.json({success:true,message:'Account activity history cleared'})}catch(e){next(e)}});
router.post('/support/tickets/:id/reply',async(req,res,next)=>{try{return res.status(501).json({success:false,message:'Ticket replies require support-agent messaging storage and are not enabled for users yet.'})}catch(e){next(e)}});
module.exports=router;
