const express = require('express');
const bcrypt = require('bcryptjs');
const jwt = require('jsonwebtoken');
const crypto = require('crypto');
const { pool } = require('../config/database');
const { sendOtpEmail, sendResetEmail } = require('../services/emailService');
const { authenticator } = require('otplib');
const router = express.Router();
const JWT_SECRET = process.env.JWT_SECRET;
const IS_PRODUCTION = process.env.NODE_ENV === 'production';
if (!JWT_SECRET) throw new Error('JWT_SECRET must be configured');
const generateOTP = () => String(crypto.randomInt(100000, 1000000));
const normalizeEmail = (value) => String(value || '').trim().toLowerCase();
function setUserSession(res, user) {
  const token = jwt.sign({ id: user.id, email: user.email, role: 'user', sv: Number(user.session_version || 1) }, JWT_SECRET, { expiresIn: '7d' });
  res.cookie('vexaccount_session', token, { httpOnly: true, secure: IS_PRODUCTION, sameSite: process.env.COOKIE_SAME_SITE || 'lax', maxAge: 604800000, path: '/' });
  return token;
}
router.post('/register', async (req,res,next)=>{const c=await pool.getConnection();try{const {email,password,name,firstName,lastName,gender,dob,country}=req.body,e=normalizeEmail(email);if(!e||!password||!name)return res.status(400).json({success:false,message:'Email, password and name are required.'});if(String(password).length<8)return res.status(400).json({success:false,message:'Password must be at least 8 characters.'});const [existing]=await c.query('SELECT id,is_verified FROM store_users WHERE email=?',[e]);if(existing.length){if(existing[0].is_verified===0){const code=generateOTP();await c.query('DELETE FROM otp_codes WHERE user_id=? AND purpose="email_verification"',[existing[0].id]);await c.query('INSERT INTO otp_codes (user_id,otp_code,purpose,expires_at) VALUES (?, ?, "email_verification", ?)',[existing[0].id,code,new Date(Date.now()+600000)]);try{await sendOtpEmail(e,code);return res.status(200).json({success:true,action:'verify',verificationRequired:true,email:e,message:'Your account already exists but is not verified. A new verification code has been sent.'});}catch(error){return res.status(200).json({success:true,action:'verify',verificationRequired:true,email:e,emailDelivery:false,message:'Your account already exists but is not verified. Open verification and use Resend code when email service is available.'});}}return res.status(409).json({success:false,message:'An account with this email already exists. Please sign in or reset your password.'});}const hash=await bcrypt.hash(String(password),12);const [result]=await c.query('INSERT INTO store_users (email,password,name,is_verified,first_name,last_name,gender,dob,country,created_at) VALUES (?, ?, ?, 0, ?, ?, ?, ?, ?, NOW())',[e,hash,String(name).trim(),firstName||null,lastName||null,gender||null,dob||null,country||null]);const code=generateOTP();await c.query('INSERT INTO otp_codes (user_id,otp_code,purpose,expires_at) VALUES (?, ?, "email_verification", ?)',[result.insertId,code,new Date(Date.now()+600000)]);try{await sendOtpEmail(e,code);return res.status(201).json({success:true,action:'verify',verificationRequired:true,email:e,data:{id:result.insertId},message:'Account created successfully. Verify your email to continue.'});}catch(error){return res.status(200).json({success:true,action:'verify',verificationRequired:true,email:e,emailDelivery:false,data:{id:result.insertId},message:'Account created successfully, but the verification email could not be sent. Continue to verification and use Resend code when email service is available.'});}}catch(error){next(error)}finally{c.release()}});
router.post('/resend-otp',async(req,res,next)=>{try{const email=normalizeEmail(req.body.email);if(!email)return res.status(400).json({success:false,message:'Enter your email address first.'});const [users]=await pool.query('SELECT id,is_verified FROM store_users WHERE email=?',[email]);if(!users.length)return res.status(404).json({success:false,message:'No VexaAccount was found for this email.'});if(users[0].is_verified)return res.status(409).json({success:false,message:'This email is already verified. Please sign in.'});const code=generateOTP();await pool.query('DELETE FROM otp_codes WHERE user_id=? AND purpose="email_verification"',[users[0].id]);await pool.query('INSERT INTO otp_codes (user_id,otp_code,purpose,expires_at) VALUES (?, ?, "email_verification", ?)',[users[0].id,code,new Date(Date.now()+600000)]);await sendOtpEmail(email,code);return res.json({success:true,message:'A new verification code was sent to your email.'});}catch(error){next(error)}});
router.post('/verify-otp',async(req,res,next)=>{const c=await pool.getConnection();try{const email=normalizeEmail(req.body.email),code=String(req.body.otp||'').trim();if(!email||!/^[0-9]{6}$/.test(code))return res.status(400).json({success:false,message:'Email and a valid 6-digit verification code are required.'});const [users]=await c.query('SELECT id,email,name,is_active,is_verified,session_version FROM store_users WHERE email=? LIMIT 1',[email]);if(!users.length)return res.status(404).json({success:false,message:'User not found.'});const user=users[0];if(!user.is_active)return res.status(403).json({success:false,message:'Account disabled.'});if(user.is_verified)return res.status(409).json({success:false,message:'Email is already verified. Please sign in.'});const [codes]=await c.query('SELECT id FROM otp_codes WHERE user_id=? AND otp_code=? AND purpose="email_verification" AND is_used=0 AND expires_at>NOW() ORDER BY id DESC LIMIT 1',[user.id,code]);if(!codes.length)return res.status(400).json({success:false,message:'Invalid or expired verification code.'});await c.beginTransaction();await c.query('UPDATE otp_codes SET is_used=1 WHERE id=?',[codes[0].id]);await c.query('UPDATE store_users SET is_verified=1 WHERE id=?',[user.id]);await c.commit();const token=setUserSession(res,user);return res.json({success:true,token,user:{id:user.id,email:user.email,name:user.name,is_verified:1}});}catch(error){await c.rollback().catch(()=>{});next(error)}finally{c.release()}});
router.post('/forgot-password',async(req,res,next)=>{try{const email=normalizeEmail(req.body.email);if(!email)return res.status(400).json({success:false,message:'Enter your email address first.'});const [users]=await pool.query('SELECT id,email FROM store_users WHERE email=?',[email]);if(!users.length)return res.json({success:true,message:'If this email is registered, you will receive a reset link.'});const user=users[0],token=jwt.sign({id:user.id,email:user.email,purpose:'password_reset'},JWT_SECRET,{expiresIn:'1h'});await pool.query('INSERT INTO otp_codes (user_id,otp_code,purpose,expires_at) VALUES (?, ?, "password_reset", DATE_ADD(NOW(),INTERVAL 1 HOUR)) ON DUPLICATE KEY UPDATE otp_code=VALUES(otp_code),expires_at=VALUES(expires_at),is_used=0',[user.id,token]);const base=String(process.env.FRONTEND_USER_URL||'https://vexaaccount-management.onrender.com').replace(/\/$/,'');try{await sendResetEmail(email,`${base}/#/reset-password?token=${encodeURIComponent(token)}`)}catch(error){return res.status(502).json({success:false,message:'We found your account, but could not send the reset email right now.'})}return res.json({success:true,message:'Reset link sent. Check your email.'});}catch(error){next(error)}});
router.post('/reset-password',async(req,res,next)=>{const c=await pool.getConnection();try{const token=String(req.body?.token||'').trim(),password=String(req.body?.password||''),confirmPassword=String(req.body?.confirmPassword||req.body?.confirm||'');if(!token||!password)return res.status(400).json({success:false,message:'Reset token and new password are required.'});if(password.length<8)return res.status(400).json({success:false,message:'Password must be at least 8 characters.'});if(confirmPassword&&password!==confirmPassword)return res.status(400).json({success:false,message:'Passwords do not match.'});let claims;try{claims=jwt.verify(token,JWT_SECRET)}catch{return res.status(400).json({success:false,message:'This reset link is invalid or has expired.'})}if (claims.purpose !== 'password_reset' || !(claims.id || claims.sub))return res.status(400).json({success:false,message:'This reset link is invalid.'});const userId=claims.id||claims.sub;const [codes]=await c.query('SELECT id FROM otp_codes WHERE user_id=? AND otp_code=? AND purpose="password_reset" AND is_used=0 AND expires_at>NOW() ORDER BY id DESC LIMIT 1',[userId,token]);if(!codes.length)return res.status(400).json({success:false,message:'This reset link has already been used or expired.'});await c.beginTransaction();await c.query('UPDATE store_users SET password=?,session_version=COALESCE(session_version,1)+1,session_version_changed_at=NOW(6) WHERE id=? AND is_active=1',[await bcrypt.hash(password,12),userId]);await c.query('UPDATE otp_codes SET is_used=1 WHERE id=?',[codes[0].id]);await c.query('UPDATE sso_sessions SET revoked_at=NOW() WHERE user_id=? AND revoked_at IS NULL',[userId]);await c.query('INSERT INTO sso_security_events (user_id,event_type,metadata) VALUES (?,?,?)',[userId,'account.password_reset',JSON.stringify({source:'email_reset'})]);await c.commit();res.clearCookie('vexaccount_session',{httpOnly:true,secure:IS_PRODUCTION,sameSite:process.env.COOKIE_SAME_SITE||'lax',path:'/'});return res.json({success:true,message:'Password reset successfully. Please sign in with your new password.'});}catch(error){await c.rollback().catch(()=>{});next(error)}finally{c.release()}});
router.post('/login',async(req,res,next)=>{try{const email=normalizeEmail(req.body.email),password=String(req.body.password||'');if(!email||!password)return res.status(400).json({success:false,message:'Email and password are required.'});const [rows]=await pool.query('SELECT * FROM store_users WHERE email=? LIMIT 1',[email]);if(!rows.length)return res.status(401).json({success:false,message:'Email or password is incorrect.'});const user=rows[0];if(!user.is_verified)return res.status(403).json({success:false,message:'Please verify your email first.',action:'verify',email:user.email});if(!user.is_active)return res.status(403).json({success:false,message:'Your account is disabled. Please contact support.'});if(!await bcrypt.compare(password,user.password||''))return res.status(401).json({success:false,message:'Email or password is incorrect.'});if(user.twofa_enabled===1&&user.twofa_secret)return res.json({success:true,requiresAuthenticator2fa:true,userId:user.id,message:'Authenticator 2FA verification required.'});if(user.email_2fa_enabled===1){const code=generateOTP();await pool.query('UPDATE otp_codes SET is_used=1 WHERE user_id=? AND purpose="email_2fa" AND is_used=0',[user.id]);await pool.query('INSERT INTO otp_codes (user_id,otp_code,purpose,expires_at) VALUES (?, ?, "email_2fa", ?)',[user.id,code,new Date(Date.now()+600000)]);try{await sendOtpEmail(user.email,code)}catch(error){return res.status(502).json({success:false,message:'Your password is correct, but we could not send the sign-in verification code.'})}return res.json({success:true,requiresEmail2fa:true,userId:user.id,message:'Sign-in verification code sent to your email.'});}const token=setUserSession(res,user);return res.json({success:true,token,user:{id:user.id,email:user.email,name:user.name,is_verified:user.is_verified}});}catch(error){next(error)}});
router.post('/verify-email-2fa',async(req,res,next)=>{const c=await pool.getConnection();try{const userId=Number(req.body.userId),email=normalizeEmail(req.body.email),code=String(req.body.otp||'').trim();if(!userId||!email||!/^[0-9]{6}$/.test(code))return res.status(400).json({success:false,message:'All verification fields are required.'});const [u]=await c.query('SELECT id,email,name,is_active,is_verified,session_version FROM store_users WHERE id=? AND email=? LIMIT 1',[userId,email]);if(!u.length||!u[0].is_active)return res.status(401).json({success:false,message:'Invalid authentication request.'});const [o]=await c.query('SELECT id FROM otp_codes WHERE user_id=? AND otp_code=? AND purpose="email_2fa" AND is_used=0 AND expires_at>NOW() ORDER BY id DESC LIMIT 1',[userId,code]);if(!o.length)return res.status(400).json({success:false,message:'Invalid or expired verification code.'});await c.query('UPDATE otp_codes SET is_used=1 WHERE id=?',[o[0].id]);const token=setUserSession(res,u[0]);return res.json({success:true,token,user:u[0]});}catch(error){next(error)}finally{c.release()}});
router.post('/twofa/verify',async(req,res,next)=>{try{const userId=Number(req.body.userId),code=String(req.body.token||'').trim();if(!userId||!code)return res.status(400).json({success:false,message:'User ID and authenticator code are required.'});const [rows]=await pool.query('SELECT id,email,name,twofa_secret,is_active,session_version FROM store_users WHERE id=? LIMIT 1',[userId]);if(!rows.length||!rows[0].is_active)return res.status(404).json({success:false,message:'User not found.'});const user=rows[0];if(!user.twofa_secret)return res.status(400).json({success:false,message:'Authenticator 2FA is not enabled.'});if(!authenticator.verify({token:code,secret:user.twofa_secret}))return res.status(400).json({success:false,message:'Invalid authenticator code.'});const token=setUserSession(res,user);return res.json({success:true,token,user});}catch(error){next(error)}});
module.exports=router;
