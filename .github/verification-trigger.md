# CI verification trigger

This marker intentionally triggers the repository verification and production runtime smoke workflows after the verification contracts were aligned with the current authentication/session-version implementation.
